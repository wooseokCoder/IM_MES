<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.board.Notification">
    <!-- [selectBordList] 사용자 알림 목록 조회 -->
    <select id="selectBordList" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_select_user_noti(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>
    
    <!-- [updateNotiRead] 알림 읽음 처리 -->
    <update id="updateNotiRead" statementType="CALLABLE" parameterType="params">
        {CALL sp_board_notification_update_read(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>
    
    

</mapper>
