<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
============================================================================
파일명: TSTD_EMPLOYEE_QUERY.xml
설명: 작업자 조회 매퍼 (SYS_USER 테이블 기반)
원본: ProActive TSTD_EMPLOYEE_QUERY.cs
작성일: 2026-03-05
============================================================================
TSTD_EMPLOYEE → SYS_USER 테이블로 전환
TSTD_EMPLOYEE.EMP_CODE → SYS_USER.USER_ID
TSTD_EMPLOYEE.EMP_NAME → SYS_USER.USER_NAME
============================================================================
-->
<mapper namespace="com.wsc.imes.std.TSTD_EMPLOYEE_QUERY">

    <!--
    ============================================================
    TSTD_EMPLOYEE_QUERY1: 작업자 기본 조회
    원본: TSTD_EMPLOYEE_QUERY.TSTD_EMPLOYEE_QUERY1()
    변환: TSTD_EMPLOYEE → SYS_USER
    ============================================================
    -->
    <select id="TSTD_EMPLOYEE_QUERY1" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tstd_employee_query1(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{empCode, mode=IN, jdbcType=VARCHAR},
            #{empName, mode=IN, jdbcType=VARCHAR},
            #{orgCode, mode=IN, jdbcType=VARCHAR},
            #{orgLike, mode=IN, jdbcType=VARCHAR},
            #{usrgrpCode, mode=IN, jdbcType=VARCHAR},
            #{accPwd, mode=IN, jdbcType=VARCHAR},
            #{availMc, mode=IN, jdbcType=VARCHAR},
            #{empLike, mode=IN, jdbcType=VARCHAR},
            #{empType, mode=IN, jdbcType=VARCHAR},
            #{rfidNo, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!--
    ============================================================
    TSTD_EMPLOYEE_QUERY5: 작업자 조회 (등록자/수정자명 포함)
    원본: TSTD_EMPLOYEE_QUERY.TSTD_EMPLOYEE_QUERY5()
    변환: TSTD_EMPLOYEE → SYS_USER
    ============================================================
    -->
    <select id="TSTD_EMPLOYEE_QUERY5" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tstd_employee_query5(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{empCode, mode=IN, jdbcType=VARCHAR},
            #{empName, mode=IN, jdbcType=VARCHAR},
            #{orgCode, mode=IN, jdbcType=VARCHAR},
            #{availMc, mode=IN, jdbcType=VARCHAR},
            #{empLike, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=INTEGER},
            #{isSystem, mode=IN, jdbcType=INTEGER},
            #{isOrg, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!--
    ============================================================
    TSTD_EMPLOYEE_QUERY6: 작업자 목록 조회
    원본: TSTD_EMPLOYEE_QUERY.TSTD_EMPLOYEE_QUERY6()
    용도: POP32A_SER2 (작업자 선택 팝업)
    변환: TSTD_EMPLOYEE → SYS_USER
    ============================================================
    -->
    <select id="TSTD_EMPLOYEE_QUERY6" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tstd_employee_query6(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{empCode, mode=IN, jdbcType=VARCHAR},
            #{orgCode, mode=IN, jdbcType=VARCHAR},
            #{empType, mode=IN, jdbcType=VARCHAR},
            #{mainMc, mode=IN, jdbcType=VARCHAR},
            #{availMc, mode=IN, jdbcType=VARCHAR},
            #{empLike, mode=IN, jdbcType=VARCHAR},
            #{isAssy, mode=IN, jdbcType=VARCHAR},
            #{isMc, mode=IN, jdbcType=VARCHAR},
            #{mcGroup, mode=IN, jdbcType=VARCHAR},
            #{empGubun2, mode=IN, jdbcType=VARCHAR},
            #{notP2, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!--
    ============================================================
    TSTD_EMPLOYEE_QUERY7: 작업자 코드만 조회 (설정 확인용)
    원본: TSTD_EMPLOYEE_QUERY.TSTD_EMPLOYEE_QUERY7()
    변환: TSTD_EMPLOYEE → SYS_USER
    ============================================================
    -->
    <select id="TSTD_EMPLOYEE_QUERY7" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tstd_employee_query7(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{empCode, mode=IN, jdbcType=VARCHAR},
            #{orgCode, mode=IN, jdbcType=VARCHAR},
            #{regEmp, mode=IN, jdbcType=VARCHAR},
            #{var, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!--
    ============================================================
    TSTD_EMPLOYEE_QUERY8: 작업자 조회 (공정명 포함)
    원본: TSTD_EMPLOYEE_QUERY.TSTD_EMPLOYEE_QUERY8()
    변환: TSTD_EMPLOYEE → SYS_USER
    ============================================================
    -->
    <select id="TSTD_EMPLOYEE_QUERY8" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tstd_employee_query8(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{empCode, mode=IN, jdbcType=VARCHAR},
            #{empName, mode=IN, jdbcType=VARCHAR},
            #{orgCode, mode=IN, jdbcType=VARCHAR},
            #{usrgrpCode, mode=IN, jdbcType=VARCHAR},
            #{orgLike, mode=IN, jdbcType=VARCHAR},
            #{accPwd, mode=IN, jdbcType=VARCHAR},
            #{availMc, mode=IN, jdbcType=VARCHAR},
            #{empLike, mode=IN, jdbcType=VARCHAR},
            #{empType, mode=IN, jdbcType=VARCHAR},
            #{rfidNo, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=INTEGER}
        )}
    </select>

</mapper>
