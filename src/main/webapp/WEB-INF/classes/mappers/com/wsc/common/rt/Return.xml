<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.rt.Return">

    <!-- ========================================================== -->
    <!--   1. 조회 - 프로시저 호출                                   -->
    <!-- ========================================================== -->

    <!-- [search] ORDR_MAST 목록 조회 (페이징 + 동적 정렬) -->
    <select id="search" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_return_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR},
            #{roType, mode=IN, jdbcType=VARCHAR},
            #{selectDate, mode=IN, jdbcType=VARCHAR},
            #{ORDR_DATE_FR, mode=IN, jdbcType=VARCHAR},
            #{ORDR_DATE_TO, mode=IN, jdbcType=VARCHAR},
            #{ORDR_NO, mode=IN, jdbcType=VARCHAR},
            #{ORDR_NO_SAP, mode=IN, jdbcType=VARCHAR},
            #{dealCode, mode=IN, jdbcType=VARCHAR},
            #{SHIP_SERI_NO, mode=IN, jdbcType=VARCHAR},
            #{Statkey, mode=IN, jdbcType=VARCHAR},
            #{SHIP_LOC, mode=IN, jdbcType=VARCHAR},
            #{CHAR_BM, mode=IN, jdbcType=VARCHAR},
            #{Mainkey, mode=IN, jdbcType=VARCHAR},
            #{TYPE_ORDR, mode=IN, jdbcType=VARCHAR},
            #{TYPE_ORDR_OP, mode=IN, jdbcType=VARCHAR},
            #{ITEM_MODL, mode=IN, jdbcType=VARCHAR},
            #{CH_YN, mode=IN, jdbcType=VARCHAR},
            #{REF_NO, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchCount] ORDR_MAST 카운트 -->
    <select id="searchCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_return_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{roType, mode=IN, jdbcType=VARCHAR},
            #{selectDate, mode=IN, jdbcType=VARCHAR},
            #{ORDR_DATE_FR, mode=IN, jdbcType=VARCHAR},
            #{ORDR_DATE_TO, mode=IN, jdbcType=VARCHAR},
            #{ORDR_NO, mode=IN, jdbcType=VARCHAR},
            #{ORDR_NO_SAP, mode=IN, jdbcType=VARCHAR},
            #{dealCode, mode=IN, jdbcType=VARCHAR},
            #{SHIP_SERI_NO, mode=IN, jdbcType=VARCHAR},
            #{Statkey, mode=IN, jdbcType=VARCHAR},
            #{SHIP_LOC, mode=IN, jdbcType=VARCHAR},
            #{CHAR_BM, mode=IN, jdbcType=VARCHAR},
            #{ITEM_MODL, mode=IN, jdbcType=VARCHAR},
            #{CH_YN, mode=IN, jdbcType=VARCHAR},
            #{REF_NO, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [select] 단건 조회 -->
    <select id="select" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectItem] 아이템 조회 -->
    <select id="selectItem" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_select_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectTransItem] Trans 아이템 조회 -->
    <select id="selectTransItem" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_select_trans_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectSwapItem] Swap 아이템 조회 -->
    <select id="selectSwapItem" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_select_swap_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getRetnOrdrNo] 주문번호 생성 -->
    <select id="getRetnOrdrNo" statementType="CALLABLE" resultType="String" parameterType="params">
        {CALL sp_return_get_retn_ordr_no(
            #{roType, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   2. INSERT - 프로시저 호출                                 -->
    <!-- ========================================================== -->

    <!-- [ordrMastInsert] ORDR_MAST 등록 -->
    <insert id="ordrMastInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_ordr_mast_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{fromDealCode, mode=IN, jdbcType=VARCHAR},
            #{fromShipTo, mode=IN, jdbcType=VARCHAR},
            #{fromShipToName, mode=IN, jdbcType=VARCHAR},
            #{fromShipToAddress1, mode=IN, jdbcType=VARCHAR},
            #{fromShipToPobox, mode=IN, jdbcType=VARCHAR},
            #{fromShipToCity, mode=IN, jdbcType=VARCHAR},
            #{fromShipToZip, mode=IN, jdbcType=VARCHAR},
            #{fromShipToCountry, mode=IN, jdbcType=VARCHAR},
            #{fromShipToLzone, mode=IN, jdbcType=VARCHAR},
            #{fromShipToState, mode=IN, jdbcType=VARCHAR},
            #{fromShipToTel, mode=IN, jdbcType=VARCHAR},
            #{fromShipToTelext, mode=IN, jdbcType=VARCHAR},
            #{fromShipToMob, mode=IN, jdbcType=VARCHAR},
            #{fromShipToFax, mode=IN, jdbcType=VARCHAR},
            #{fromShipToEmail, mode=IN, jdbcType=VARCHAR},
            #{custPoNo, mode=IN, jdbcType=VARCHAR},
            #{payMeth, mode=IN, jdbcType=VARCHAR},
            #{INV_FILE_INFO, mode=IN, jdbcType=VARCHAR},
            #{ORDR_CLS_DATE, mode=IN, jdbcType=VARCHAR},
            #{roType, mode=IN, jdbcType=VARCHAR},
            #{eoSts, mode=IN, jdbcType=VARCHAR},
            #{estRetnDate, mode=IN, jdbcType=VARCHAR},
            #{retnLoc, mode=IN, jdbcType=VARCHAR},
            #{retnLocType, mode=IN, jdbcType=VARCHAR},
            #{retnDoNo, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [retnMastInsert] RETN_MAST 등록 -->
    <insert id="retnMastInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_retn_mast_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{roType, mode=IN, jdbcType=VARCHAR},
            #{trsfResnRd, mode=IN, jdbcType=VARCHAR},
            #{retnDoNo, mode=IN, jdbcType=VARCHAR},
            #{retnSoNo, mode=IN, jdbcType=VARCHAR},
            #{reference, mode=IN, jdbcType=VARCHAR},
            #{retnLoc, mode=IN, jdbcType=VARCHAR},
            #{retnLocType, mode=IN, jdbcType=VARCHAR},
            #{retnLocAddr, mode=IN, jdbcType=VARCHAR},
            #{retnLocZip, mode=IN, jdbcType=VARCHAR},
            #{retnLocCity, mode=IN, jdbcType=VARCHAR},
            #{retnLocLzone, mode=IN, jdbcType=VARCHAR},
            #{retnLocCnty, mode=IN, jdbcType=VARCHAR},
            #{estRetnDate, mode=IN, jdbcType=VARCHAR},
            #{toDealCode, mode=IN, jdbcType=VARCHAR},
            #{toShipToName, mode=IN, jdbcType=VARCHAR},
            #{toShipToAddress1, mode=IN, jdbcType=VARCHAR},
            #{toShipToPobox, mode=IN, jdbcType=VARCHAR},
            #{toShipToZip, mode=IN, jdbcType=VARCHAR},
            #{toShipToCountry, mode=IN, jdbcType=VARCHAR},
            #{toShipToCity, mode=IN, jdbcType=VARCHAR},
            #{toShipToLzone, mode=IN, jdbcType=VARCHAR},
            #{toShipToState, mode=IN, jdbcType=VARCHAR},
            #{toShipToTel, mode=IN, jdbcType=VARCHAR},
            #{toShipToTelext, mode=IN, jdbcType=VARCHAR},
            #{toShipToMob, mode=IN, jdbcType=VARCHAR},
            #{toShipToFax, mode=IN, jdbcType=VARCHAR},
            #{toShipToEmail, mode=IN, jdbcType=VARCHAR},
            #{retlDate, mode=IN, jdbcType=VARCHAR},
            #{trsfDate, mode=IN, jdbcType=VARCHAR},
            #{RdDealerAppr, mode=IN, jdbcType=VARCHAR},
            #{chk_wf, mode=IN, jdbcType=VARCHAR},
            #{chk_deal, mode=IN, jdbcType=VARCHAR},
            #{chk_ls, mode=IN, jdbcType=VARCHAR},
            #{retnResn, mode=IN, jdbcType=VARCHAR},
            #{trsfResn, mode=IN, jdbcType=VARCHAR},
            #{ffrNo, mode=IN, jdbcType=VARCHAR},
            #{swapYn, mode=IN, jdbcType=VARCHAR},
            #{transYn, mode=IN, jdbcType=VARCHAR},
            #{gradYn, mode=IN, jdbcType=VARCHAR},
            #{instDesc, mode=IN, jdbcType=VARCHAR},
            #{remk, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [ordrItemInsert] ORDR_ITEM 등록 -->
    <insert id="ordrItemInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_ordr_item_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{ITEM_TYPE, mode=IN, jdbcType=VARCHAR},
            #{ITEM_MODL, mode=IN, jdbcType=VARCHAR},
            #{ITEM_CODE, mode=IN, jdbcType=VARCHAR},
            #{ITEM_NAME, mode=IN, jdbcType=VARCHAR},
            #{SERI_NO, mode=IN, jdbcType=VARCHAR},
            #{VIN_NO, mode=IN, jdbcType=VARCHAR},
            #{GROS_AMT, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [retnItemInsert] RETN_ITEM 등록 -->
    <insert id="retnItemInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_retn_item_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{USE_HOUR, mode=IN, jdbcType=VARCHAR},
            #{CRED_AMT, mode=IN, jdbcType=VARCHAR},
            #{RE_BILL_PRCE, mode=IN, jdbcType=VARCHAR},
            #{TERM_GE_LST, mode=IN, jdbcType=VARCHAR},
            #{FLOR_PERD, mode=IN, jdbcType=VARCHAR},
            #{RETL_INTV, mode=IN, jdbcType=VARCHAR},
            #{ACC_COMT, mode=IN, jdbcType=VARCHAR},
            #{NOT_ACC_COMT, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [retnTransInsert] RETN_TRANS_ITEM 등록 -->
    <insert id="retnTransInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_retn_trans_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{ITEM_MODL, mode=IN, jdbcType=VARCHAR},
            #{SHIP_SERI_NO, mode=IN, jdbcType=VARCHAR},
            #{ITEM_QTY, mode=IN, jdbcType=INTEGER},
            #{HOUR, mode=IN, jdbcType=VARCHAR},
            #{BEF_INFO, mode=IN, jdbcType=VARCHAR},
            #{AFT_INFO, mode=IN, jdbcType=VARCHAR},
            #{ITEM_AMT, mode=IN, jdbcType=VARCHAR},
            #{SYS_REMK, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [retnSwapInsert] RETN_SWAP_ITEM 등록 -->
    <insert id="retnSwapInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_retn_swap_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{ITEM_MODL, mode=IN, jdbcType=VARCHAR},
            #{SHIP_SERI_NO, mode=IN, jdbcType=VARCHAR},
            #{ITEM_QTY, mode=IN, jdbcType=INTEGER},
            #{OLD_SERI_NO, mode=IN, jdbcType=VARCHAR},
            #{NEW_SERI_NO, mode=IN, jdbcType=VARCHAR},
            #{SYS_REMK, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- ========================================================== -->
    <!--   3. DELETE - 프로시저 호출                                 -->
    <!-- ========================================================== -->

    <!-- [ordrItemDelete] ORDR_ITEM 삭제 -->
    <delete id="ordrItemDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_ordr_item_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [retnItemDelete] RETN_ITEM 삭제 -->
    <delete id="retnItemDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_retn_item_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [ordrMastDelete] ORDR_MAST 삭제 -->
    <delete id="ordrMastDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_ordr_mast_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [retnMastDelete] RETN_MAST 삭제 -->
    <delete id="retnMastDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_retn_mast_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [retnSwapDelete] RETN_SWAP_ITEM 삭제 -->
    <delete id="retnSwapDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_retn_swap_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [retnTransDelete] RETN_TRANS_ITEM 삭제 -->
    <delete id="retnTransDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_retn_trans_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   4. 주소/주문 조회 - 프로시저 호출                          -->
    <!-- ========================================================== -->

    <!-- [searchAddr] 주소 조회 -->
    <select id="searchAddr" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_search_addr(
            #{DEAL_CODE, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchOrder] 주문 조회 -->
    <select id="searchOrder" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_search_order(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{refNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchOrderHist] 주문 이력 조회 -->
    <select id="searchOrderHist" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_search_order_hist(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{refNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchOrderSeriNo] 시리얼번호로 주문 조회 -->
    <select id="searchOrderSeriNo" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_search_order_seri_no(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{refNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchOrderSeriNoHist] 시리얼번호로 이력 조회 -->
    <select id="searchOrderSeriNoHist" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_search_order_seri_no_hist(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{refNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   5. UPDATE - 프로시저 호출                                 -->
    <!-- ========================================================== -->

    <!-- [ordrInfoUpdate] ORDR_MAST 정보 수정 -->
    <update id="ordrInfoUpdate" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_ordr_info_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{fromDealCode, mode=IN, jdbcType=VARCHAR},
            #{fromShipTo, mode=IN, jdbcType=VARCHAR},
            #{fromShipToName, mode=IN, jdbcType=VARCHAR},
            #{fromShipToAddress1, mode=IN, jdbcType=VARCHAR},
            #{fromShipToPobox, mode=IN, jdbcType=VARCHAR},
            #{fromShipToCity, mode=IN, jdbcType=VARCHAR},
            #{fromShipToZip, mode=IN, jdbcType=VARCHAR},
            #{fromShipToCountry, mode=IN, jdbcType=VARCHAR},
            #{fromShipToLzone, mode=IN, jdbcType=VARCHAR},
            #{fromShipToState, mode=IN, jdbcType=VARCHAR},
            #{fromShipToTel, mode=IN, jdbcType=VARCHAR},
            #{fromShipToTelext, mode=IN, jdbcType=VARCHAR},
            #{fromShipToMob, mode=IN, jdbcType=VARCHAR},
            #{fromShipToFax, mode=IN, jdbcType=VARCHAR},
            #{fromShipToEmail, mode=IN, jdbcType=VARCHAR},
            #{custPoNo, mode=IN, jdbcType=VARCHAR},
            #{payMeth, mode=IN, jdbcType=VARCHAR},
            #{INV_FILE_INFO, mode=IN, jdbcType=VARCHAR},
            #{ORDR_CLS_DATE, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [retnMastUpdate] RETN_MAST 수정 -->
    <update id="retnMastUpdate" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_retn_mast_update(
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{retnDoNo, mode=IN, jdbcType=VARCHAR},
            #{retnSoNo, mode=IN, jdbcType=VARCHAR},
            #{reference, mode=IN, jdbcType=VARCHAR},
            #{toDealCode, mode=IN, jdbcType=VARCHAR},
            #{toShipToName, mode=IN, jdbcType=VARCHAR},
            #{toShipToAddress1, mode=IN, jdbcType=VARCHAR},
            #{toShipToPobox, mode=IN, jdbcType=VARCHAR},
            #{toShipToZip, mode=IN, jdbcType=VARCHAR},
            #{toShipToCountry, mode=IN, jdbcType=VARCHAR},
            #{toShipToCity, mode=IN, jdbcType=VARCHAR},
            #{toShipToLzone, mode=IN, jdbcType=VARCHAR},
            #{toShipToState, mode=IN, jdbcType=VARCHAR},
            #{toShipToTel, mode=IN, jdbcType=VARCHAR},
            #{toShipToTelext, mode=IN, jdbcType=VARCHAR},
            #{toShipToMob, mode=IN, jdbcType=VARCHAR},
            #{toShipToFax, mode=IN, jdbcType=VARCHAR},
            #{toShipToEmail, mode=IN, jdbcType=VARCHAR},
            #{retlDate, mode=IN, jdbcType=VARCHAR},
            #{trsfDate, mode=IN, jdbcType=VARCHAR},
            #{RdDealerAppr, mode=IN, jdbcType=VARCHAR},
            #{chk_wf, mode=IN, jdbcType=VARCHAR},
            #{chk_deal, mode=IN, jdbcType=VARCHAR},
            #{chk_ls, mode=IN, jdbcType=VARCHAR},
            #{retnResn, mode=IN, jdbcType=VARCHAR},
            #{trsfResnRd, mode=IN, jdbcType=VARCHAR},
            #{trsfResn, mode=IN, jdbcType=VARCHAR},
            #{swapYn, mode=IN, jdbcType=VARCHAR},
            #{transYn, mode=IN, jdbcType=VARCHAR},
            #{gradYn, mode=IN, jdbcType=VARCHAR},
            #{ffrNo, mode=IN, jdbcType=VARCHAR},
            #{instDesc, mode=IN, jdbcType=VARCHAR},
            #{remk, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [ordrMastUpdateOrg] ORDR_MAST 원본 기반 수정 -->
    <update id="ordrMastUpdateOrg" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_ordr_mast_update_org(
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{retnDoNo, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [ordrMastUpdateOrgHist] ORDR_MAST 이력 기반 수정 -->
    <update id="ordrMastUpdateOrgHist" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_ordr_mast_update_org_hist(
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [ordrMastUpdateNotOrg] ORDR_MAST 원본 없이 수정 -->
    <update id="ordrMastUpdateNotOrg" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_ordr_mast_update_not_org(
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- ========================================================== -->
    <!--   6. 워크플로우/메일 - 기존 프로시저 호출 유지               -->
    <!-- ========================================================== -->

    <!-- [getWFInfo] 워크플로우 정보 조회 -->
    <select id="getWFInfo" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_get_wf_info(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [update_Sts_Wf_Info] 상태/워크플로우 정보 수정 (기존 프로시저) -->
    <select id="update_Sts_Wf_Info" resultType="record" parameterType="params">
        CALL SP_RETN_UPDATE_STS_WF(#{sysId},#{gsUserId},#{ordrNo}, #{apprYn}, #{apprComt})
    </select>

    <!-- [createTO] TO 생성 (기존 프로시저) -->
    <select id="createTO" resultType="record" parameterType="params">
        CALL SP_RETN_CREATE_TO(#{sysId},#{gsUserId},#{ordrNo})
    </select>

    <!-- [getRetnReviewMailInfo] 반품 리뷰 메일 정보 (기존 프로시저) -->
    <select id="getRetnReviewMailInfo" resultType="record" parameterType="params">
        CALL SP_ORDR_DETAIL_GET_RETN_REVIEW_MAIL_INFO(#{sysId},#{gsUserId},#{gsLang}, #{ordrNo})
    </select>

    <!-- [getRetnMailMainInfo] 반품 메일 메인 정보 (기존 프로시저) -->
    <select id="getRetnMailMainInfo" resultType="record" parameterType="params">
        CALL SP_RETN_MAIL_GET_MAIN_ITEM_LIST(#{sysId},#{gsUserId},#{gsLang}, #{ordrNo}, 'MAIN')
    </select>

    <!-- [getRetnMailTrInfo] 반품 메일 TR 정보 (기존 프로시저) -->
    <select id="getRetnMailTrInfo" resultType="record" parameterType="params">
        CALL SP_RETN_MAIL_GET_MAIN_ITEM_LIST(#{sysId},#{gsUserId},#{gsLang}, #{ordrNo}, 'TR')
    </select>

    <!-- [getRetnMailAttaInfo] 반품 메일 첨부 정보 (기존 프로시저) -->
    <select id="getRetnMailAttaInfo" resultType="record" parameterType="params">
        CALL SP_RETN_MAIL_GET_MAIN_ITEM_LIST(#{sysId},#{gsUserId},#{gsLang}, #{ordrNo}, 'ATTA')
    </select>

    <!-- [getOderInfo] 주문 정보 조회 -->
    <select id="getOderInfo" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_get_oder_info(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   7. 체크 조회 - 프로시저 호출                              -->
    <!-- ========================================================== -->

    <!-- [getCheckItem] 체크 아이템 조회 -->
    <select id="getCheckItem" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_get_check_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getCheckMiss] 체크 누락 파트 조회 -->
    <select id="getCheckMiss" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_get_check_miss(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getGrad] 등급 조회 -->
    <select id="getGrad" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_get_grad(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getCheckInfo] 체크 정보 조회 -->
    <select id="getCheckInfo" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_get_check_info(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getCheckAttLL] 체크 첨부 LL 조회 -->
    <select id="getCheckAttLL" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_get_check_att_ll(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getCheckAttLB] 체크 첨부 LB 조회 -->
    <select id="getCheckAttLB" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_get_check_att_lb(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getCheckAttOTH] 체크 첨부 OTH 조회 -->
    <select id="getCheckAttOTH" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_return_get_check_att_oth(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   8. 체크 마스터 CRUD - 프로시저 호출                       -->
    <!-- ========================================================== -->

    <!-- [mastSelectCnt] 마스터 카운트 -->
    <select id="mastSelectCnt" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_return_mast_select_cnt(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [mastUpdate] 마스터 수정 -->
    <update id="mastUpdate" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_mast_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{wsDate, mode=IN, jdbcType=VARCHAR},
            #{checkDate, mode=IN, jdbcType=VARCHAR},
            #{trmodel, mode=IN, jdbcType=VARCHAR},
            #{chassis, mode=IN, jdbcType=VARCHAR},
            #{engine, mode=IN, jdbcType=VARCHAR},
            #{workH, mode=IN, jdbcType=VARCHAR},
            #{INSP_TOT_GRAD, mode=IN, jdbcType=VARCHAR},
            #{INSP_GRAD_A, mode=IN, jdbcType=VARCHAR},
            #{INSP_GRAD_B, mode=IN, jdbcType=VARCHAR},
            #{INSP_GRAD_C, mode=IN, jdbcType=VARCHAR},
            #{RETN_REMK, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [mastInsert] 마스터 등록 -->
    <insert id="mastInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_mast_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{wsDate, mode=IN, jdbcType=VARCHAR},
            #{checkDate, mode=IN, jdbcType=VARCHAR},
            #{trmodel, mode=IN, jdbcType=VARCHAR},
            #{chassis, mode=IN, jdbcType=VARCHAR},
            #{engine, mode=IN, jdbcType=VARCHAR},
            #{workH, mode=IN, jdbcType=VARCHAR},
            #{INSP_TOT_GRAD, mode=IN, jdbcType=VARCHAR},
            #{INSP_GRAD_A, mode=IN, jdbcType=VARCHAR},
            #{INSP_GRAD_B, mode=IN, jdbcType=VARCHAR},
            #{INSP_GRAD_C, mode=IN, jdbcType=VARCHAR},
            #{RETN_REMK, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- ========================================================== -->
    <!--   9. 등급 CRUD - 프로시저 호출                              -->
    <!-- ========================================================== -->

    <!-- [gradSelectCnt] 등급 카운트 -->
    <select id="gradSelectCnt" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_return_grad_select_cnt(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [gradUpdate] 등급 수정 -->
    <update id="gradUpdate" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_grad_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{grad_EVAL_TYPE, mode=IN, jdbcType=VARCHAR},
            #{grad_EVAL_SEQ, mode=IN, jdbcType=INTEGER},
            #{grad_EVAL_PONT, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [gradInsert] 등급 등록 -->
    <insert id="gradInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_grad_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{grad_EVAL_TYPE, mode=IN, jdbcType=VARCHAR},
            #{grad_EVAL_SEQ, mode=IN, jdbcType=INTEGER},
            #{grad_formatNo, mode=IN, jdbcType=VARCHAR},
            #{grad_EVAL_PONT, mode=IN, jdbcType=VARCHAR},
            #{EVAL_IDX, mode=IN, jdbcType=VARCHAR},
            #{EVAL_REMK, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- ========================================================== -->
    <!--   10. 체크 아이템 CRUD - 프로시저 호출                      -->
    <!-- ========================================================== -->

    <!-- [checkItemSelectCnt] 체크 아이템 카운트 -->
    <select id="checkItemSelectCnt" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_return_check_item_select_cnt(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [checkItemUpdate] 체크 아이템 수정 -->
    <update id="checkItemUpdate" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_check_item_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{ci_CHEK_SEQ, mode=IN, jdbcType=INTEGER},
            #{ci_CHEK_STAT, mode=IN, jdbcType=VARCHAR},
            #{ci_CHEK_DECN, mode=IN, jdbcType=VARCHAR},
            #{ci_ABNO_CONT, mode=IN, jdbcType=VARCHAR},
            #{ci_CHEK_REMK, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [checkItemInsert] 체크 아이템 등록 -->
    <insert id="checkItemInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_check_item_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{ci_CHEK_SEQ, mode=IN, jdbcType=INTEGER},
            #{ci_formatNo, mode=IN, jdbcType=VARCHAR},
            #{ci_CHEK_STAT, mode=IN, jdbcType=VARCHAR},
            #{ci_CHEK_DECN, mode=IN, jdbcType=VARCHAR},
            #{ci_ABNO_CONT, mode=IN, jdbcType=VARCHAR},
            #{ci_CHEK_REMK, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- ========================================================== -->
    <!--   11. 체크 첨부 CRUD - 프로시저 호출                        -->
    <!-- ========================================================== -->

    <!-- [checkAttaSelectCnt] 체크 첨부 카운트 -->
    <select id="checkAttaSelectCnt" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_return_check_atta_select_cnt(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [checkAttaUpdate] 체크 첨부 수정 -->
    <update id="checkAttaUpdate" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_check_atta_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{att_itemType, mode=IN, jdbcType=VARCHAR},
            #{att_model, mode=IN, jdbcType=VARCHAR},
            #{att_seriNo, mode=IN, jdbcType=VARCHAR},
            #{att_bucket, mode=IN, jdbcType=VARCHAR},
            #{att_remark, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [checkAttaInsert] 체크 첨부 등록 -->
    <insert id="checkAttaInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_check_atta_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{att_itemType, mode=IN, jdbcType=VARCHAR},
            #{att_model, mode=IN, jdbcType=VARCHAR},
            #{att_seriNo, mode=IN, jdbcType=VARCHAR},
            #{att_bucket, mode=IN, jdbcType=VARCHAR},
            #{att_remark, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- ========================================================== -->
    <!--   12. 체크 파트 CRUD - 프로시저 호출                        -->
    <!-- ========================================================== -->

    <!-- [checkPartSelectCnt] 체크 파트 카운트 -->
    <select id="checkPartSelectCnt" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_return_check_part_select_cnt(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [checkPartdelete] 체크 파트 삭제 -->
    <delete id="checkPartdelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_check_part_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [checkPartInsert] 체크 파트 등록 -->
    <insert id="checkPartInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_check_part_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{part_ITEM_CODE, mode=IN, jdbcType=VARCHAR},
            #{part_ITEM_NAME, mode=IN, jdbcType=VARCHAR},
            #{part_ITEM_QTY, mode=IN, jdbcType=INTEGER},
            #{part_ITEM_CONT, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- ========================================================== -->
    <!--   13. 기타 삭제 - 프로시저 호출                             -->
    <!-- ========================================================== -->

    <!-- [UpdateCheckAppr] 체크 승인자 수정 -->
    <update id="UpdateCheckAppr" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_update_check_appr(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{retnNo, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [chekMastDelete] 체크 마스터 삭제 -->
    <delete id="chekMastDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_chek_mast_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [chekAttaDelete] 체크 첨부 삭제 -->
    <delete id="chekAttaDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_chek_atta_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [chekGradDelete] 체크 등급 삭제 -->
    <delete id="chekGradDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_chek_grad_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [chekItemDelete] 체크 아이템 삭제 -->
    <delete id="chekItemDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_chek_item_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [chekPartDelete] 체크 파트 삭제 -->
    <delete id="chekPartDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_return_chek_part_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   14. 딜러 주소 조회 - 기존 프로시저 호출 유지               -->
    <!-- ========================================================== -->

    <!-- [getRetnDealAddr] 딜러 주소 조회 (기존 프로시저) -->
    <select id="getRetnDealAddr" resultType="record" parameterType="params">
        CALL SP_ORDR_DETAIL_DL_GET_SHIP_ADDR(#{sysId}, #{dealCode}, #{gsLang})
    </select>

    <!-- [getRetnWHAddr] 창고 주소 조회 (기존 프로시저) -->
    <select id="getRetnWHAddr" resultType="record" parameterType="params">
        CALL SP_ORDR_DETAIL_DL_GET_SHIP_ADDR(#{sysId}, #{dealCode}, #{gsLang})
    </select>

</mapper>
