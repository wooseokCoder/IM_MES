<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
============================================================================
파일명: TORD_EMAIL_GROUP_QUERY.xml
설명: 이메일 수신자그룹 조회 매퍼 (TORD_EMAIL_GROUP 테이블)
작성자: 송우석
작성일: 2026-02-10
============================================================================
-->
<mapper namespace="com.wsc.imes.ord.TORD_EMAIL_GROUP_QUERY">

    <!--
    ============================================================
    TORD_EMAIL_GROUP_QUERY1: 수신자그룹 목록 조회
    용도: 메인 그리드1 (좌측 그룹 목록)
    조건: GROUP_TYPE='A' 고정, DATA_FLAG 필터
    ============================================================
    -->
    <select id="TORD_EMAIL_GROUP_QUERY1" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tord_email_group_query1(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=INTEGER},
            #{groupType, mode=IN, jdbcType=VARCHAR},
            #{mcode, mode=IN, jdbcType=VARCHAR},
            #{groupLike, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
