<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.user2.Group2">

    <!-- ========================================================== -->
    <!--   1. 그룹 관리 (SYS_GRUP) - 프로시저 호출                   -->
    <!-- ========================================================== -->

    <!-- [search] 그룹 목록 조회 (페이징 + 동적 정렬) -->
    <!-- gsSorts: List<String>을 bind로 콤마 구분 문자열 생성 후 프로시저에 전달 -->
    <select id="search" statementType="CALLABLE" parameterType="params" resultType="group">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_group2_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{groupName, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchCount] 그룹 목록 카운트 -->
    <select id="searchCount" statementType="CALLABLE" parameterType="params" resultType="int">
        {CALL sp_group2_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{groupName, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [select] 그룹 단건 조회 -->
    <select id="select" statementType="CALLABLE" parameterType="params" resultType="group">
        {CALL sp_group2_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insert] 그룹 등록 -->
    <insert id="insert" statementType="CALLABLE" parameterType="params">
        {CALL sp_group2_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{groupName, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [update] 그룹 수정 -->
    <update id="update" statementType="CALLABLE" parameterType="params">
        {CALL sp_group2_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{groupName, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [delete] 그룹 삭제 -->
    <delete id="delete" statementType="CALLABLE" parameterType="params">
        {CALL sp_group2_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   2. 사용자-그룹 관리 (SYS_UGRP) - 프로시저 호출            -->
    <!-- ========================================================== -->

    <!-- [searchUserGroup] 사용자-그룹 목록 조회 (페이징 + 동적 정렬) -->
    <!-- gsSorts: List<String>을 bind로 콤마 구분 문자열 생성 후 프로시저에 전달 -->
    <select id="searchUserGroup" statementType="CALLABLE" parameterType="params" resultType="record">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_group2_search_user_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchUserGroupCount] 사용자-그룹 목록 카운트 -->
    <select id="searchUserGroupCount" statementType="CALLABLE" parameterType="params" resultType="int">
        {CALL sp_group2_search_user_group_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectUserGroup] 사용자-그룹 단건 조회 -->
    <select id="selectUserGroup" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_group2_select_user_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insertUserGroup] 사용자-그룹 등록 -->
    <insert id="insertUserGroup" statementType="CALLABLE" parameterType="params">
        {CALL sp_group2_insert_user_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [updateUserGroup] 사용자-그룹 수정 -->
    <update id="updateUserGroup" statementType="CALLABLE" parameterType="params">
        {CALL sp_group2_update_user_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [deleteUserGroup] 사용자-그룹 삭제 -->
    <delete id="deleteUserGroup" statementType="CALLABLE" parameterType="params">
        {CALL sp_group2_delete_user_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

</mapper>
