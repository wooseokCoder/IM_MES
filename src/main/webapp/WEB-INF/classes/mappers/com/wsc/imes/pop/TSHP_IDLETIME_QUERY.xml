<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
============================================================================
파일명: TSHP_IDLETIME_QUERY.xml
설명: 비가동 실적 조회 매퍼 (POB 모듈)
     TSHP_IDLETIME + LSE_MACHINE + TSTD_EMPLOYEE + TSTD_IDLECODE JOIN
원본: ProActive TSHP_IDLETIME_QUERY.cs - TSHP_IDLETIME_QUERY2
화면: POB32A (비가동 현황 - 조립)
작성일: 2026-03-04
============================================================================
-->
<mapper namespace="com.wsc.imes.pop.TSHP_IDLETIME_QUERY">

    <!--
    ============================================================
    TSHP_IDLETIME_QUERY2: 비가동 현황 목록 조회
    원본: TSHP_IDLETIME_QUERY.TSHP_IDLETIME_QUERY2()
    용도: POP32A 메인 그리드 조회 (POP32A_SER 서비스)
    JOIN: LSE_MACHINE(설비명), TSTD_EMPLOYEE(작업자명/등록자명), TSTD_IDLECODE(비가동명)
    파라미터:
      p_plt_code    - 공장코드 (gsPltCode)
      p_s_idle_date - 비가동 시작일 (yyyyMMdd)
      p_e_idle_date - 비가동 종료일 (yyyyMMdd)
      p_mc_group    - 설비그룹 (AS-IS: PLANTS 값 복사 → gsPltCode 동일)
      p_data_flag   - 데이터 상태 (0=활성, 2=삭제)
    정렬: 시작시간 역순
    ============================================================
    -->
    <select id="TSHP_IDLETIME_QUERY2" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tshp_idletime_query2(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{idleId, mode=IN, jdbcType=VARCHAR},
            #{mcCode, mode=IN, jdbcType=VARCHAR},
            #{empCode, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=VARCHAR},
            #{sIdleDate, mode=IN, jdbcType=VARCHAR},
            #{eIdleDate, mode=IN, jdbcType=VARCHAR},
            #{sWorkDate, mode=IN, jdbcType=VARCHAR},
            #{eWorkDate, mode=IN, jdbcType=VARCHAR},
            #{idleState, mode=IN, jdbcType=VARCHAR},
            #{mcGroup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!--
    ============================================================
    TSHP_IDLETIME_QUERY7: 진행중 비가동 조회
    원본: TSHP_IDLETIME_QUERY.TSHP_IDLETIME_QUERY7()
    용도: ORD16A 완료처리 시 해당 작업지시(WO_NO)에 대해
          현재 진행중(IDLE_STATE=1)인 비가동 레코드를 조회
    파라미터: gsPltCode, woNo, idleState
    ============================================================
    -->
    <select id="TSHP_IDLETIME_QUERY7" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tshp_idletime_query7(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{woNo, mode=IN, jdbcType=VARCHAR},
            #{idleState, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!--
    ============================================================
    TSHP_IDLETIME_QUERY2_1: 비가동 조회
    원본: TSHP_IDLETIME_QUERY.TSHP_IDLETIME_QUERY2()
    용도: POP30B 설비 기준 비가동 현황 조회
    WHERE: AS-IS GetWhere 9개 전체 반영
    추가일: 2026-03-18
    수정일: 2026-03-19 (AS-IS WHERE 전체 반영)
    ============================================================
    -->
    <select id="TSHP_IDLETIME_QUERY2_1" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tshp_idletime_query2_1(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{idleId, mode=IN, jdbcType=VARCHAR},
            #{mcCode, mode=IN, jdbcType=VARCHAR},
            #{empCode, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=VARCHAR},
            #{sIdleDate, mode=IN, jdbcType=VARCHAR},
            #{eIdleDate, mode=IN, jdbcType=VARCHAR},
            #{sWorkDate, mode=IN, jdbcType=VARCHAR},
            #{eWorkDate, mode=IN, jdbcType=VARCHAR},
            #{idleState, mode=IN, jdbcType=VARCHAR},
            #{mcGroup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
