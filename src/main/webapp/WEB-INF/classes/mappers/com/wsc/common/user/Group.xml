<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.user.Group">

    <!-- ========================================================== -->
    <!--   Group Management                                         -->
    <!-- ========================================================== -->

    <!-- 1. 그룹 목록 조회 (페이징) -->
    <select id="search" resultType="group" parameterType="params" statementType="CALLABLE">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_group_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{groupName, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER},
            #{sortStr, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 2. 그룹 목록 카운트 -->
    <select id="searchCount" resultType="int" parameterType="params" statementType="CALLABLE">
        {CALL sp_group_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{groupName, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 3. 그룹 단건 조회 -->
    <select id="select" resultType="group" parameterType="params" statementType="CALLABLE">
        {CALL sp_group_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 4. 그룹 등록 -->
    <insert id="insert" parameterType="params" statementType="CALLABLE">
        {CALL sp_group_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{groupName, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- 5. 그룹 수정 -->
    <update id="update" parameterType="params" statementType="CALLABLE">
        {CALL sp_group_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{groupName, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- 6. 그룹 삭제 -->
    <delete id="delete" parameterType="params" statementType="CALLABLE">
        {CALL sp_group_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   User Group Management                                    -->
    <!-- ========================================================== -->

    <!-- 7. 사용자-그룹 목록 조회 (페이징) -->
    <select id="searchUserGroup" resultType="record" parameterType="params" statementType="CALLABLE">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_usergroup_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER},
            #{sortStr, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 8. 사용자-그룹 목록 카운트 -->
    <select id="searchUserGroupCount" resultType="int" parameterType="params" statementType="CALLABLE">
        {CALL sp_usergroup_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 9. 사용자-그룹 단건 조회 -->
    <select id="selectUserGroup" resultType="record" parameterType="params" statementType="CALLABLE">
        {CALL sp_usergroup_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 10. 사용자-그룹 등록 -->
    <insert id="insertUserGroup" parameterType="params" statementType="CALLABLE">
        {CALL sp_usergroup_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- 11. 사용자-그룹 수정 -->
    <update id="updateUserGroup" parameterType="params" statementType="CALLABLE">
        {CALL sp_usergroup_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{groupId2, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- 12. 사용자-그룹 삭제 -->
    <delete id="deleteUserGroup" parameterType="params" statementType="CALLABLE">
        {CALL sp_usergroup_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- 13. 사용자 목록 조회 -->
    <select id="selectUserList" resultType="record" parameterType="params" statementType="CALLABLE">
        {CALL sp_group_select_user_list(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 14. 그룹 목록 조회 (콤보용) -->
    <select id="selectGroupList" resultType="record" parameterType="params" statementType="CALLABLE">
        {CALL sp_group_select_list(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 15. 프로그램 목록 조회 (콤보용) - 기존 프로시저 유지 -->
    <select id="selectUserProgList" resultType="record" parameterType="params" statementType="CALLABLE">
        {CALL sp_search_Program_List_Combo(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
