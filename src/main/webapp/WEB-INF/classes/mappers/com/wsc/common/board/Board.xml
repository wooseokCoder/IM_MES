<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.board.Board">

    <!-- ========================================================== -->
    <!--   1. 게시글 관리 (SYS_BORD) - 프로시저 호출                  -->
    <!-- ========================================================== -->

    <!-- [search] 게시글 목록 조회 (페이징 및 동적 검색) -->
    <!-- gsSorts: List<String>을 bind로 콤마 구분 문자열 생성 후 프로시저에 전달 -->
    <select id="search" statementType="CALLABLE" parameterType="params" resultType="record">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_board_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{regiId, mode=IN, jdbcType=VARCHAR},
            #{bordTitle, mode=IN, jdbcType=VARCHAR},
            #{bordText, mode=IN, jdbcType=LONGVARCHAR},
            #{bordType, mode=IN, jdbcType=VARCHAR},
            #{bordBgn, mode=IN, jdbcType=VARCHAR},
            #{bordEnd, mode=IN, jdbcType=VARCHAR},
            #{openType, mode=IN, jdbcType=VARCHAR},
            #{searchGrp1, mode=IN, jdbcType=VARCHAR},
            #{searchGrp2, mode=IN, jdbcType=VARCHAR},
            #{searchGrp3, mode=IN, jdbcType=VARCHAR},
            #{searchGrp4, mode=IN, jdbcType=VARCHAR},
            #{language, mode=IN, jdbcType=VARCHAR},
            #{searchKey, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR},
            #{date1, mode=IN, jdbcType=VARCHAR},
            #{date2, mode=IN, jdbcType=VARCHAR},
            #{bordPno, mode=IN, jdbcType=VARCHAR},
            #{todayYn, mode=IN, jdbcType=VARCHAR},
            #{notAnyMore, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{gsOrgAuthCode, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchCount] 게시글 카운트 -->
    <select id="searchCount" statementType="CALLABLE" parameterType="params" resultType="int">
        {CALL sp_board_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{regiId, mode=IN, jdbcType=VARCHAR},
            #{bordTitle, mode=IN, jdbcType=VARCHAR},
            #{bordText, mode=IN, jdbcType=LONGVARCHAR},
            #{bordType, mode=IN, jdbcType=VARCHAR},
            #{bordBgn, mode=IN, jdbcType=VARCHAR},
            #{bordEnd, mode=IN, jdbcType=VARCHAR},
            #{openType, mode=IN, jdbcType=VARCHAR},
            #{searchGrp1, mode=IN, jdbcType=VARCHAR},
            #{searchGrp2, mode=IN, jdbcType=VARCHAR},
            #{searchGrp3, mode=IN, jdbcType=VARCHAR},
            #{searchGrp4, mode=IN, jdbcType=VARCHAR},
            #{language, mode=IN, jdbcType=VARCHAR},
            #{searchKey, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR},
            #{date1, mode=IN, jdbcType=VARCHAR},
            #{date2, mode=IN, jdbcType=VARCHAR},
            #{bordPno, mode=IN, jdbcType=VARCHAR},
            #{todayYn, mode=IN, jdbcType=VARCHAR},
            #{notAnyMore, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{gsOrgAuthCode, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [select] 게시글 상세 조회 -->
    <select id="select" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insert] 게시글 등록 -->
    <select id="insert" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordTitle, mode=IN, jdbcType=VARCHAR},
            #{bordType, mode=IN, jdbcType=VARCHAR},
            #{bordBgn, mode=IN, jdbcType=VARCHAR},
            #{bordEnd, mode=IN, jdbcType=VARCHAR},
            #{bordPno, mode=IN, jdbcType=VARCHAR},
            #{openType, mode=IN, jdbcType=VARCHAR},
            #{bordText, mode=IN, jdbcType=BLOB},
            #{dataGrp1, mode=IN, jdbcType=VARCHAR},
            #{dataGrp2, mode=IN, jdbcType=VARCHAR},
            #{dataGrp3, mode=IN, jdbcType=VARCHAR},
            #{dataGrp4, mode=IN, jdbcType=VARCHAR},
            #{dataSer1, mode=IN, jdbcType=VARCHAR},
            #{dataSer2, mode=IN, jdbcType=VARCHAR},
            #{dataSer3, mode=IN, jdbcType=VARCHAR},
            #{dataSer4, mode=IN, jdbcType=VARCHAR},
            #{dataSer5, mode=IN, jdbcType=VARCHAR},
            #{dataMd01, mode=IN, jdbcType=VARCHAR},
            #{dataMd02, mode=IN, jdbcType=VARCHAR},
            #{dataMd03, mode=IN, jdbcType=VARCHAR},
            #{dataMd04, mode=IN, jdbcType=VARCHAR},
            #{dataMd05, mode=IN, jdbcType=VARCHAR},
            #{dataMd06, mode=IN, jdbcType=VARCHAR},
            #{dataMd07, mode=IN, jdbcType=VARCHAR},
            #{dataMd08, mode=IN, jdbcType=VARCHAR},
            #{dataMd09, mode=IN, jdbcType=VARCHAR},
            #{dataMd10, mode=IN, jdbcType=VARCHAR},
            #{searIdx, mode=IN, jdbcType=VARCHAR},
            #{justOne, mode=IN, jdbcType=VARCHAR},
            #{LANGUAGE, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [update] 게시글 수정 -->
    <update id="update" statementType="CALLABLE" parameterType="params">
        {CALL sp_board_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordTitle, mode=IN, jdbcType=VARCHAR},
            #{bordText, mode=IN, jdbcType=BLOB},
            #{bordType, mode=IN, jdbcType=VARCHAR},
            #{bordBgn, mode=IN, jdbcType=VARCHAR},
            #{bordEnd, mode=IN, jdbcType=VARCHAR},
            #{readCnt, mode=IN, jdbcType=VARCHAR},
            #{bordSeq, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{openType, mode=IN, jdbcType=VARCHAR},
            #{dataGrp1, mode=IN, jdbcType=VARCHAR},
            #{dataGrp2, mode=IN, jdbcType=VARCHAR},
            #{dataGrp3, mode=IN, jdbcType=VARCHAR},
            #{dataGrp4, mode=IN, jdbcType=VARCHAR},
            #{dataSer1, mode=IN, jdbcType=VARCHAR},
            #{dataSer2, mode=IN, jdbcType=VARCHAR},
            #{dataSer3, mode=IN, jdbcType=VARCHAR},
            #{dataSer4, mode=IN, jdbcType=VARCHAR},
            #{dataSer5, mode=IN, jdbcType=VARCHAR},
            #{dataMd01, mode=IN, jdbcType=VARCHAR},
            #{dataMd02, mode=IN, jdbcType=VARCHAR},
            #{dataMd03, mode=IN, jdbcType=VARCHAR},
            #{dataMd04, mode=IN, jdbcType=VARCHAR},
            #{dataMd05, mode=IN, jdbcType=VARCHAR},
            #{dataMd06, mode=IN, jdbcType=VARCHAR},
            #{dataMd07, mode=IN, jdbcType=VARCHAR},
            #{dataMd08, mode=IN, jdbcType=VARCHAR},
            #{dataMd09, mode=IN, jdbcType=VARCHAR},
            #{dataMd10, mode=IN, jdbcType=VARCHAR},
            #{searIdx, mode=IN, jdbcType=VARCHAR},
            #{justOne, mode=IN, jdbcType=VARCHAR},
            #{LANGUAGE, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [delete] 게시글 물리 삭제 -->
    <delete id="delete" statementType="CALLABLE" parameterType="params">
        {CALL sp_board_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [updateReadCnt] 조회수 증가 -->
    <update id="updateReadCnt" statementType="CALLABLE" parameterType="params">
        {CALL sp_board_update_read_cnt(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [updateDisable] 게시글 사용안함 처리 -->
    <update id="updateDisable" statementType="CALLABLE" parameterType="params">
        {CALL sp_board_update_disable(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- ========================================================== -->
    <!--   2. 대상자 관리 (SYS_BORD_TGT) - 프로시저 호출              -->
    <!-- ========================================================== -->

    <!-- [searchTarget] 대상자 목록 조회 -->
    <!-- gsSorts: List<String>을 bind로 콤마 구분 문자열 생성 후 프로시저에 전달 -->
    <select id="searchTarget" statementType="CALLABLE" parameterType="params" resultType="record">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_board_search_target(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{vndrCode, mode=IN, jdbcType=VARCHAR},
            #{vndrName, mode=IN, jdbcType=VARCHAR},
            #{saveIdx, mode=IN, jdbcType=VARCHAR},
            #{pageType, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchTargetCount] 대상자 목록 카운트 -->
    <select id="searchTargetCount" statementType="CALLABLE" parameterType="params" resultType="int">
        {CALL sp_board_search_target_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{vndrCode, mode=IN, jdbcType=VARCHAR},
            #{vndrName, mode=IN, jdbcType=VARCHAR},
            #{saveIdx, mode=IN, jdbcType=VARCHAR},
            #{pageType, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectTarget] 대상자 단건 조회 -->
    <select id="selectTarget" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_select_target(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insertTarget] 대상자 등록 -->
    <!-- targets: 배열 또는 List를 bind로 콤마 구분 문자열 생성 후 프로시저에 전달 -->
    <insert id="insertTarget" statementType="CALLABLE" parameterType="params">
        <bind name="targetStr" value="@com.wsc.framework.utils.MybatisUtils@joinAny(',', targets)" />
        {CALL sp_board_insert_target(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{targetStr, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{readYn, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [updateTarget] 대상자 정보 수정 -->
    <update id="updateTarget" statementType="CALLABLE" parameterType="params">
        {CALL sp_board_update_target(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{vndrCode, mode=IN, jdbcType=VARCHAR},
            #{vndrName, mode=IN, jdbcType=VARCHAR},
            #{saveIdx, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [updateTargetDisable] 대상자 비활성화 처리 -->
    <update id="updateTargetDisable" statementType="CALLABLE" parameterType="params">
        {CALL sp_board_update_target_disable(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [deleteTargetAll] 대상자 일괄 삭제 -->
    <delete id="deleteTargetAll" statementType="CALLABLE" parameterType="params">
        {CALL sp_board_delete_target_all(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [updateTargetRead] 대상자 읽음 처리 -->
    <update id="updateTargetRead" statementType="CALLABLE" parameterType="params">
        {CALL sp_board_update_target_read(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- ========================================================== -->
    <!--   3. 대시보드/인덱스 - 프로시저 호출                        -->
    <!-- ========================================================== -->

    <!-- [alterTop] 알림 상단 조회 -->
    <select id="alterTop" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_alter_top(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [alterBottom] 알림 하단 조회 -->
    <select id="alterBottom" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_alter_bottom(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [referemceTop] 참고자료 상단 조회 -->
    <select id="referemceTop" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_reference_top(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [referemceBottom] 참고자료 하단 조회 -->
    <select id="referemceBottom" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_reference_bottom(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [noticeTop] 공지사항 상단 조회 -->
    <select id="noticeTop" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_notice_top(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchBordDetail] 게시판 상세 조회 -->
    <select id="searchBordDetail" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_search_bord_detail(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchViewsList] 조회자 목록 -->
    <select id="searchViewsList" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_search_views_list(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{searchKey, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchViewsListCount] 조회자 목록 카운트 -->
    <select id="searchViewsListCount" statementType="CALLABLE" parameterType="params" resultType="int">
        {CALL sp_board_search_views_list_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchBordIndexB17] B17 게시판 인덱스 조회 -->
    <select id="searchBordIndexB17" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_search_bord_index_b17()}
    </select>

    <!-- [searchBordIndexB99] B99 게시판 인덱스 조회 (기존 프로시저 유지) -->
    <select id="searchBordIndexB99" resultType="record" parameterType="params">
        CALL sp_search_index_bord(#{sysId}, #{gsUserId})
    </select>

    <!-- [searchBordIndexB19] B19 게시판 인덱스 조회 -->
    <select id="searchBordIndexB19" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_search_bord_index_b19()}
    </select>

    <!-- [searchBordImg] 게시판 이미지 조회 -->
    <select id="searchBordImg" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_search_bord_img(
            #{ATCH_GRUP, mode=IN, jdbcType=VARCHAR},
            #{ATCH_NO, mode=IN, jdbcType=VARCHAR},
            #{fileImagePath, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchBordTc] 대시보드 통계 (기존 프로시저 유지) -->
    <select id="searchBordTc" resultType="record" parameterType="params">
        CALL SP_DASH_GET_ORDR_STAT_CNT(#{sysId},#{gsUserId},'en',#{gsOrgAuthCode})
    </select>

    <!-- [searchBordAgTc] 대시보드 AG 통계 (기존 프로시저 유지) -->
    <select id="searchBordAgTc" resultType="record" parameterType="params">
        CALL SP_DASH_GET_ORDR_AG_STAT_CNT(#{sysId},#{gsUserId},'en',#{gsOrgAuthCode})
    </select>

    <!-- [searchBordAgTc04] 대시보드 AG04 통계 (기존 프로시저 유지) -->
    <select id="searchBordAgTc04" resultType="record" parameterType="params">
        CALL SP_DASH_GET_ORDR_AG_STAT04_CNT(#{sysId},#{gsUserId},'en',#{gsOrgAuthCode})
    </select>

    <!-- ========================================================== -->
    <!--   4. 매뉴얼/기타 - 프로시저 호출                            -->
    <!-- ========================================================== -->

    <!-- [getManualSeries] 매뉴얼 시리즈 목록 -->
    <select id="getManualSeries" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_get_manual_series(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getManualModel] 매뉴얼 모델 목록 -->
    <select id="getManualModel" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_get_manual_model(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getRegiUserId] 등록자 ID 조회 -->
    <select id="getRegiUserId" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_board_get_regi_user_id(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [dealerCheck] 딜러 존재 여부 확인 -->
    <!-- dealerList: List<String>을 bind로 콤마 구분 문자열 생성 후 프로시저에 전달 -->
    <select id="dealerCheck" statementType="CALLABLE" parameterType="params" resultType="int">
        <bind name="dealerListStr" value="@com.wsc.framework.utils.MybatisUtils@joinAny(',', dealerList)" />
        {CALL sp_board_dealer_check(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{dealerListStr, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [editCheck] 수정 권한 체크 -->
    <select id="editCheck" statementType="CALLABLE" parameterType="params" resultType="string">
        {CALL sp_board_edit_check(
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchChartBizPlan] 사업 계획 차트 (기존 프로시저 유지) -->
    <select id="searchChartBizPlan" resultType="record" parameterType="params">
        CALL sp_search_chart_biz_plan(#{sysId}, #{gsUserId})
    </select>

    <!-- [searchDailySalesReport] 일일 판매 리포트 (기존 프로시저 유지) -->
    <select id="searchDailySalesReport" resultType="record" parameterType="params">
        CALL sp_search_daily_sale_report(#{sysId}, #{gsUserId})
    </select>

    <!-- [getDashDealInfo] 대시보드 딜러 정보 (기존 프로시저 유지) -->
    <select id="getDashDealInfo" resultType="record" parameterType="params">
        CALL sp_get_dash_deal_info(#{sysId}, #{gsUserId})
    </select>

    <!-- [searchBordRts04] 대시보드 RTS04 (기존 프로시저 유지) -->
    <select id="searchBordRts04" resultType="record" parameterType="params">
        CALL SP_DASH_GET_ORDR_STR04(#{sysId},#{gsUserId},'en',#{gsOrgAuthCode})
    </select>

</mapper>
