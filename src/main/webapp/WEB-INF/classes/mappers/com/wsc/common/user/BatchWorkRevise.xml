<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.user.BatchWorkRevise">

    <!-- ========================================================== -->
    <!--   BatchWorkRevise - 프로시저 호출 방식                       -->
    <!--   생성일: 2026-01-15                                        -->
    <!-- ========================================================== -->

    <!-- [search] 배치 작업 수정 목록 조회 (페이징, 동적 정렬) -->
    <select id="search" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_batchworkrevise_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=IN, jdbcType=VARCHAR},
            #{jobGrup, mode=IN, jdbcType=VARCHAR},
            #{jobTerm, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchCount] 배치 작업 수정 카운트 -->
    <select id="searchCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_batchworkrevise_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=IN, jdbcType=VARCHAR},
            #{jobGrup, mode=IN, jdbcType=VARCHAR},
            #{jobTerm, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [BatchWorkReviseinsertJob] 배치 작업 등록 (프로시저에서 ID 자동생성 + INSERT 한번에 처리) -->
    <insert id="BatchWorkReviseinsertJob" statementType="CALLABLE" parameterType="params">
        {CALL sp_batchworkrevise_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobGrup, mode=IN, jdbcType=VARCHAR},
            #{jobType, mode=IN, jdbcType=VARCHAR},
            #{jobTerm, mode=IN, jdbcType=VARCHAR},
            #{jobTime, mode=IN, jdbcType=VARCHAR},
            #{jobCmd, mode=IN, jdbcType=VARCHAR},
            #{jobDesc, mode=IN, jdbcType=VARCHAR},
            #{errProc, mode=IN, jdbcType=VARCHAR},
            #{jobMng, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{jobRemk, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [BatchWorkReviseupdateJob] 배치 작업 수정 -->
    <update id="BatchWorkReviseupdateJob" statementType="CALLABLE" parameterType="params">
        {CALL sp_batchworkrevise_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=IN, jdbcType=VARCHAR},
            #{jobIdNew, mode=IN, jdbcType=VARCHAR},
            #{jobGrup, mode=IN, jdbcType=VARCHAR},
            #{jobType, mode=IN, jdbcType=VARCHAR},
            #{jobTerm, mode=IN, jdbcType=VARCHAR},
            #{jobTime, mode=IN, jdbcType=VARCHAR},
            #{jobCmd, mode=IN, jdbcType=VARCHAR},
            #{jobDesc, mode=IN, jdbcType=VARCHAR},
            #{errProc, mode=IN, jdbcType=VARCHAR},
            #{jobMng, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{jobRemk, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

</mapper>
