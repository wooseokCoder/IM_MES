<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.mail.Mail">

    <!-- ========================================================== -->
    <!--   1. 보낸 메일함 - 프로시저 호출                            -->
    <!-- ========================================================== -->

    <!-- [search] 보낸 메일함 목록 조회 -->
    <select id="search" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_mail_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{searchKey, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchCount] 보낸 메일함 카운트 -->
    <select id="searchCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_mail_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{searchKey, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [select] 메일 상세 조회 -->
    <select id="select" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_mail_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [mailNo] 메일 번호 생성 -->
    <select id="mailNo" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_mail_get_no(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   2. 메일 CRUD - 프로시저 호출                              -->
    <!-- ========================================================== -->

    <!-- [insert] 메일 등록 -->
    <insert id="insert" statementType="CALLABLE" parameterType="params">
        {CALL sp_mail_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordTitle, mode=IN, jdbcType=VARCHAR},
            #{bordText, mode=IN, jdbcType=LONGVARCHAR},
            #{bordType, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [delete] 메일 삭제 (논리 삭제) -->
    <update id="delete" statementType="CALLABLE" parameterType="params">
        {CALL sp_mail_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{mailType, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [deleteTargetAll] 대상자 전체 삭제 (논리 삭제) -->
    <update id="deleteTargetAll" statementType="CALLABLE" parameterType="params">
        {CALL sp_mail_delete_target_all(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{mailType, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [mailTgtDelete] 메일 대상자 물리 삭제 -->
    <delete id="mailTgtDelete" statementType="CALLABLE" parameterType="params">
        {CALL sp_mail_tgt_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [insertTarget] 대상자 등록 -->
    <insert id="insertTarget" statementType="CALLABLE" parameterType="params">
	    <bind name="targetStr" value="@com.wsc.framework.utils.MybatisUtils@joinAny(',', targets)" />
	    
	    { CALL sp_board_target_insert(
	        #{targetStr, mode=IN, jdbcType=VARCHAR},
	        #{sysId, mode=IN, jdbcType=VARCHAR},
	        #{bordNo, mode=IN, jdbcType=VARCHAR},
	        #{bordGrup, mode=IN, jdbcType=VARCHAR},
	        #{gsUserId, mode=IN, jdbcType=VARCHAR}
	    ) }
	</insert>

    <!-- [updateReadMail] 메일 읽음 처리 -->
    <update id="updateReadMail" statementType="CALLABLE" parameterType="params">
        {CALL sp_mail_update_read(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [updateFailMail] 메일 실패 처리 -->
    <update id="updateFailMail" statementType="CALLABLE" parameterType="params">
        {CALL sp_mail_update_fail(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [selectTarget] 메일 대상자 상세 조회 -->
    <select id="selectTarget" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_mail_select_target(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   3. 주소록 관리 - 프로시저 호출                            -->
    <!-- ========================================================== -->

    <!-- [searchUserAddress] 주소록 목록 조회 -->
    <select id="searchUserAddress" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_mail_search_user_address(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{searchKey, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchUserAddressCount] 주소록 카운트 -->
    <select id="searchUserAddressCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_mail_search_user_address_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectUserAddress] 주소록 단건 조회 -->
    <select id="selectUserAddress" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_mail_select_user_address(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insertUserAddress] 주소록 등록 -->
    <insert id="insertUserAddress" statementType="CALLABLE" parameterType="params">
        {CALL sp_mail_insert_user_address(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserName, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [updateUserAddress] 주소록 수정 -->
    <update id="updateUserAddress" statementType="CALLABLE" parameterType="params">
        {CALL sp_mail_update_user_address(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserName, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [deleteUserAddress] 주소록 삭제 -->
    <delete id="deleteUserAddress" statementType="CALLABLE" parameterType="params">
        {CALL sp_mail_delete_user_address(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   4. 내부 사용자 / 받은 메일함 - 프로시저 호출               -->
    <!-- ========================================================== -->

    <!-- [searchUserInternalList] 내부 사용자 목록 조회 -->
    <select id="searchUserInternalList" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_mail_search_user_internal_list(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{searchKey, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchInternalList] 받은 메일함 목록 조회 -->
    <select id="searchInternalList" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_mail_search_internal_list(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{searchKey, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchInternalListCount] 받은 메일함 카운트 -->
    <select id="searchInternalListCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_mail_search_internal_list_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{searchKey, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   5. 메일 열람자 - 프로시저 호출                            -->
    <!-- ========================================================== -->

    <!-- [searchMailViewsList] 메일 열람자 목록 조회 -->
    <select id="searchMailViewsList" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_mail_search_views_list(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR},
            #{searchText, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchMailViewsListCount] 메일 열람자 카운트 -->
    <select id="searchMailViewsListCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_mail_search_views_list_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{bordNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   6. 메일 플래그 체크 - 프로시저 호출                        -->
    <!-- ========================================================== -->

    <!-- [returnMailYnCheck] Return 메일 플래그 체크 -->
    <select id="returnMailYnCheck" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL sp_mail_flag_check(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            'RETURN'
        )}
    </select>

    <!-- [confirmMailYnCheck] Confirm 메일 플래그 체크 -->
    <select id="confirmMailYnCheck" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL sp_mail_flag_check(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            'CONFIRM'
        )}
    </select>

    <!-- [bolMailYnCheck] BOL 메일 플래그 체크 -->
    <select id="bolMailYnCheck" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL sp_mail_flag_check(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            'BOL'
        )}
    </select>

    <!-- [reviewMailYnCheck] Review 메일 플래그 체크 -->
    <select id="reviewMailYnCheck" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL sp_mail_flag_check(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            'REVIEW'
        )}
    </select>

    <!-- [monthlyMailYnCheck] Monthly 메일 플래그 체크 -->
    <select id="monthlyMailYnCheck" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL sp_mail_flag_check(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            'MONTHLY'
        )}
    </select>

    <!-- [ordrClMailYnCheck] Order Close 메일 플래그 체크 -->
    <select id="ordrClMailYnCheck" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL sp_mail_flag_check(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            'ORDRCL'
        )}
    </select>

    <!-- ========================================================== -->
    <!--   7. 기타 (기존 프로시저 유지)                              -->
    <!-- ========================================================== -->

    <!-- [selectSmtpMailCheck] SMTP 메일 체크 (기존 프로시저 유지) -->
    <select id="selectSmtpMailCheck" statementType="CALLABLE" resultType="hashmap" parameterType="params">
        {CALL select_smtp_mail_check(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
