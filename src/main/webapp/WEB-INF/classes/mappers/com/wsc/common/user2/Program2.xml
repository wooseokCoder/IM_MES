<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.user2.Program2">

    <!-- ========================================================== -->
    <!--   Program Management - Stored Procedure Calls              -->
    <!-- ========================================================== -->

    <!-- [search] 프로그램 목록 조회 (페이징 및 동적 검색) -->
    <!-- gsSorts: List<String>을 bind로 콤마 구분 문자열 생성 후 프로시저에 전달 -->
    <select id="search" statementType="CALLABLE" parameterType="params" resultType="prog">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_program2_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{progName, mode=IN, jdbcType=VARCHAR},
            #{progType, mode=IN, jdbcType=VARCHAR},
            #{sysLoc, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchCount] 프로그램 목록 카운트 -->
    <select id="searchCount" statementType="CALLABLE" parameterType="params" resultType="int">
        {CALL sp_program2_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{progName, mode=IN, jdbcType=VARCHAR},
            #{progType, mode=IN, jdbcType=VARCHAR},
            #{sysLoc, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [select] 프로그램 단건 조회 -->
    <select id="select" statementType="CALLABLE" parameterType="params" resultType="prog">
        {CALL sp_program2_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insert] 프로그램 등록 -->
    <insert id="insert" statementType="CALLABLE" parameterType="params">
        {CALL sp_program2_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{progName, mode=IN, jdbcType=VARCHAR},
            #{progType, mode=IN, jdbcType=VARCHAR},
            #{sysLoc, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [update] 프로그램 수정 -->
    <update id="update" statementType="CALLABLE" parameterType="params">
        {CALL sp_program2_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{progName, mode=IN, jdbcType=VARCHAR},
            #{progType, mode=IN, jdbcType=VARCHAR},
            #{sysLoc, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [delete] 프로그램 삭제 -->
    <delete id="delete" statementType="CALLABLE" parameterType="params">
        {CALL sp_program2_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   Group Program Management - Stored Procedure Calls        -->
    <!-- ========================================================== -->

    <!-- [searchGroupProgram] 그룹-프로그램 목록 조회 (페이징) -->
    <select id="searchGroupProgram" statementType="CALLABLE" parameterType="params" resultType="prog">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_program2_search_group_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchGroupProgramCount] 그룹-프로그램 목록 카운트 -->
    <select id="searchGroupProgramCount" statementType="CALLABLE" parameterType="params" resultType="int">
        {CALL sp_program2_search_group_program_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectGroupProgram] 그룹-프로그램 단건 조회 -->
    <select id="selectGroupProgram" statementType="CALLABLE" parameterType="params" resultType="prog">
        {CALL sp_program2_select_group_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insertGroupProgram] 그룹-프로그램 등록 -->
    <insert id="insertGroupProgram" statementType="CALLABLE" parameterType="params">
        {CALL sp_program2_insert_group_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [updateGroupProgram] 그룹-프로그램 수정 -->
    <update id="updateGroupProgram" statementType="CALLABLE" parameterType="params">
        {CALL sp_program2_update_group_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [deleteGroupProgram] 그룹-프로그램 삭제 -->
    <delete id="deleteGroupProgram" statementType="CALLABLE" parameterType="params">
        {CALL sp_program2_delete_group_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   User Program Management - Stored Procedure Calls         -->
    <!-- ========================================================== -->

    <!-- [searchUserProgram] 사용자-프로그램 목록 조회 (페이징) -->
    <select id="searchUserProgram" statementType="CALLABLE" parameterType="params" resultType="prog">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_program2_search_user_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchUserProgramCount] 사용자-프로그램 목록 카운트 -->
    <select id="searchUserProgramCount" statementType="CALLABLE" parameterType="params" resultType="int">
        {CALL sp_program2_search_user_program_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectUserProgram] 사용자-프로그램 단건 조회 -->
    <select id="selectUserProgram" statementType="CALLABLE" parameterType="params" resultType="prog">
        {CALL sp_program2_select_user_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insertUserProgram] 사용자-프로그램 등록 -->
    <insert id="insertUserProgram" statementType="CALLABLE" parameterType="params">
        {CALL sp_program2_insert_user_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [updateUserProgram] 사용자-프로그램 수정 -->
    <update id="updateUserProgram" statementType="CALLABLE" parameterType="params">
        {CALL sp_program2_update_user_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tranA, mode=IN, jdbcType=VARCHAR},
            #{tranC, mode=IN, jdbcType=VARCHAR},
            #{tranR, mode=IN, jdbcType=VARCHAR},
            #{tranU, mode=IN, jdbcType=VARCHAR},
            #{tranD, mode=IN, jdbcType=VARCHAR},
            #{tranP, mode=IN, jdbcType=VARCHAR},
            #{tranS, mode=IN, jdbcType=VARCHAR},
            #{tran1, mode=IN, jdbcType=VARCHAR},
            #{tran2, mode=IN, jdbcType=VARCHAR},
            #{tran3, mode=IN, jdbcType=VARCHAR},
            #{tran4, mode=IN, jdbcType=VARCHAR},
            #{tran5, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [deleteUserProgram] 사용자-프로그램 삭제 -->
    <delete id="deleteUserProgram" statementType="CALLABLE" parameterType="params">
        {CALL sp_program2_delete_user_program(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{progId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   Program Security (V_SYS_AUTH) - Stored Procedure Call    -->
    <!-- ========================================================== -->

    <!-- [selectSecurity] 프로그램 권한정보 조회 (VIEW V_SYS_AUTH 사용) -->
    <select id="selectSecurity" statementType="CALLABLE" parameterType="params" resultType="prog">
        {CALL sp_program2_select_security(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{uri, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
