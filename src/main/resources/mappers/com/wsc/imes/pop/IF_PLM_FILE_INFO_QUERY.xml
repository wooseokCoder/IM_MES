<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
============================================================================
파일명: IF_PLM_FILE_INFO_QUERY.xml
설명: PLM 파일 정보 조회 매퍼 (IF_PLM_FILE_INFO 테이블)
원본: ProActive IF_PLM_FILE_INFO_QUERY.cs (namespace: DIF)
작성일: 2026-03-03
============================================================================
-->
<mapper namespace="com.wsc.imes.pop.IF_PLM_FILE_INFO_QUERY">

    <!--
    ============================================================
    IF_PLM_FILE_INFO_QUERY1: PLM 도면 파일 목록 조회
    용도: 도면 버튼 클릭 시 파일 목록 조회 (MAT02A 등)
    원본: IF_PLM_FILE_INFO_QUERY.IF_PLM_FILE_INFO_QUERY1
    래퍼: POP30A_SER16
    ============================================================
    -->
    <select id="IF_PLM_FILE_INFO_QUERY1" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_if_plm_file_info_query1(
            #{pltCode,   mode=IN, jdbcType=VARCHAR},
            #{catCode,   mode=IN, jdbcType=VARCHAR},
            #{ordNo,     mode=IN, jdbcType=VARCHAR},
            #{lineNo,    mode=IN, jdbcType=VARCHAR},
            #{procStat,  mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
