<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
============================================================================
파일명: TSYS_FILELIST_MASTER_QUERY.xml
설명: 파일 목록 조회 매퍼 (TSYS_FILELIST_MASTER 테이블)
원본: ProActive TSYS_FILELIST_MASTER_QUERY.cs
작성일: 2026-03-03
============================================================================
-->
<mapper namespace="com.wsc.imes.sys.TSYS_FILELIST_MASTER_QUERY">

    <!--
    ============================================================
    TSYS_FILELIST_MASTER_QUERY6: 파일 목록 조회
    용도: STD47A_SER2 (작업표준서 리스트)
    ============================================================
    -->
    <select id="TSYS_FILELIST_MASTER_QUERY6" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_tsys_filelist_master_query6(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{uploadMenu, mode=IN, jdbcType=VARCHAR},
            #{modelLike, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=TINYINT}
        )}
    </select>


    <!--
    ============================================================
    ATTACH_FILE_SER: 파일 목록 조회 (acAttachFileControl)
    용도: LINK_KEY 기준 첨부 파일 목록
    ============================================================
    -->
    <select id="ATTACH_FILE_SER" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_attach_file_ser(
            #{gsPltCode,  mode=IN, jdbcType=VARCHAR},
            #{uploadMenu, mode=IN, jdbcType=VARCHAR},
            #{linkKey,    mode=IN, jdbcType=VARCHAR},
            #{dataFlag,   mode=IN, jdbcType=TINYINT}
        )}
    </select>

    <!--
    ============================================================
    ATTACH_FILE_INS: 파일 메타정보 INSERT (acAttachFileControl)
    용도: 파일 업로드 후 DB 등록
    OUT p_FILE_ID: 채번된 FILE_ID (FL + yyMMdd + - + ####)
    ============================================================
    -->
    <insert id="ATTACH_FILE_INS" statementType="CALLABLE" parameterType="params">
        {CALL sp_imes_attach_file_ins(
            #{gsPltCode,   mode=IN,  jdbcType=VARCHAR},
            #{uploadMenu,  mode=IN,  jdbcType=VARCHAR},
            #{linkKey,     mode=IN,  jdbcType=VARCHAR},
            #{fileName,    mode=IN,  jdbcType=VARCHAR},
            #{fileSize,    mode=IN,  jdbcType=BIGINT},
            #{fileSeq,     mode=IN,  jdbcType=INTEGER},
            #{accLevel,    mode=IN,  jdbcType=VARCHAR},
            #{gsUserId,    mode=IN,  jdbcType=VARCHAR},
            #{dataFlag,    mode=IN,  jdbcType=TINYINT},
            #{fileId,      mode=OUT, jdbcType=VARCHAR}
        )}
    </insert>

    <!--
    ============================================================
    ATTACH_FILE_DEL: 파일 논리 삭제 (acAttachFileControl)
    용도: DATA_FLAG=2, DEL_EMP/DEL_DATE 기록 (원본: QUERY3 WHERE DATA_FLAG=2)
    ============================================================
    -->
    <update id="ATTACH_FILE_DEL" statementType="CALLABLE" parameterType="params">
        {CALL sp_imes_attach_file_del(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{fileId,    mode=IN, jdbcType=VARCHAR},
            #{gsUserId,  mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!--
    ============================================================
    ATTACH_FILE_GET_SEQ: 다음 FILE_SEQ 조회
    용도: 업로드 시 순서 채번
    ============================================================
    -->
    <select id="ATTACH_FILE_GET_SEQ" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_attach_file_get_seq(
            #{gsPltCode,  mode=IN,  jdbcType=VARCHAR},
            #{uploadMenu, mode=IN,  jdbcType=VARCHAR},
            #{linkKey,    mode=IN,  jdbcType=VARCHAR},
            #{nextSeq,    mode=OUT, jdbcType=INTEGER}
        )}
    </select>

    <!--
    ============================================================
    ATTACH_FILE_DEL_HISTORY_SER: 삭제이력 조회 (acAttachFileControl)
    용도: 삭제이력 탭 — DATA_FLAG=2 인 파일 목록
    원본: TSYS_FILELIST_MASTER_QUERY3 → WHERE DATA_FLAG=2
          ATTACH_FILE_MASTER_SER → RSLTDT2 (FileDelHistoryGridView)
    ============================================================
    -->
    <select id="ATTACH_FILE_DEL_HISTORY_SER" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_attach_file_del_history_ser(
            #{gsPltCode,  mode=IN, jdbcType=VARCHAR},
            #{uploadMenu, mode=IN, jdbcType=VARCHAR},
            #{linkKey,    mode=IN, jdbcType=VARCHAR}
        )}
    </select>


    <!--
    ============================================================
    ATTACH_FILE_RENAME: 파일명 변경 (이름 바꾸기)
    용도: 우클릭 컨텍스트 메뉴 "이름 바꾸기"
    원본: btnRename_ItemClick → ATTACH_FILE_MASTER_SER4
    ============================================================
    -->
    <update id="ATTACH_FILE_RENAME" statementType="CALLABLE" parameterType="params">
        {CALL sp_imes_attach_file_rename(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{fileId,    mode=IN, jdbcType=VARCHAR},
            #{fileName,  mode=IN, jdbcType=VARCHAR},
            #{gsUserId,  mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!--
    ============================================================
    ATTACH_FILE_ACC_LEVEL_UPD: 공개형태 변경 (PUBLIC / PRIVATE)
    용도: 우클릭 컨텍스트 메뉴 "공개형태"
    원본: AccLevelChange_ItemClick → ATTACH_FILE_MASTER_UPD2
    ============================================================
    -->
    <update id="ATTACH_FILE_ACC_LEVEL_UPD" statementType="CALLABLE" parameterType="params">
        {CALL sp_imes_attach_file_acc_level_upd(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{fileId,    mode=IN, jdbcType=VARCHAR},
            #{accLevel,  mode=IN, jdbcType=VARCHAR},
            #{gsUserId,  mode=IN, jdbcType=VARCHAR}
        )}
    </update>

</mapper>
