<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
============================================================================
파일명: TSHP_ACTUAL.xml
설명: 공정 실적 CUD 매퍼 (TSHP_ACTUAL 테이블) - ORD16A 완료처리 전용
원본: ProActive TSHP_ACTUAL.cs
화면: ORD16A (생산오더 완료처리)
작성일: 2026-03-04
============================================================================
-->
<mapper namespace="com.wsc.imes.ord.TSHP_ACTUAL">

    <!--
    ============================================================
    TSHP_ACTUAL_UPD9: 실적 완료 처리
    용도: ORD16A 완료처리 - PROC_STAT=4(완료), ACT_END_TIME 설정,
          IS_AUTO_IDLE_FLAG='0' (자동 비가동 플래그 초기화)
    파라미터: gsPltCode, actualId, procStat, actEndTime, gsUserId
    ============================================================
    -->
    <update id="TSHP_ACTUAL_UPD9" statementType="CALLABLE" parameterType="params">
        {CALL sp_imes_tshp_actual_upd9(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{actualId, mode=IN, jdbcType=VARCHAR},
            #{procStat, mode=IN, jdbcType=VARCHAR},
            #{actEndTime, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!--
    ============================================================
    TSHP_ACTUAL_INS2: 완료 실적 생성 (INSERT 19 columns)
    용도: ORD16A 완료처리 시 새로운 실적 레코드 생성
          작업일, 작업지시번호, 작업자, 설비, 공정상태, 패널상태,
          시작/종료시간, 자가시간, 인력시간, OT시간,
          양품수량, 불량수량, 다중시작건수, 입력구분 등
    파라미터: gsPltCode, actualId, workDate, woNo, empCode,
             mcCode, mcNmCheck, procStat, panelStat,
             actStartTime, actEndTime, selfTime, manTime, otTime,
             okQty, ngQty, multiStartCnt, inputFlag, gsUserId
    ============================================================
    -->
    <insert id="TSHP_ACTUAL_INS2" statementType="CALLABLE" parameterType="params">
        {CALL sp_imes_tshp_actual_ins2(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{actualId, mode=IN, jdbcType=VARCHAR},
            #{workDate, mode=IN, jdbcType=VARCHAR},
            #{woNo, mode=IN, jdbcType=VARCHAR},
            #{empCode, mode=IN, jdbcType=VARCHAR},
            #{mcCode, mode=IN, jdbcType=VARCHAR},
            #{mcNmCheck, mode=IN, jdbcType=VARCHAR},
            #{procStat, mode=IN, jdbcType=VARCHAR},
            #{panelStat, mode=IN, jdbcType=VARCHAR},
            #{actStartTime, mode=IN, jdbcType=VARCHAR},
            #{actEndTime, mode=IN, jdbcType=VARCHAR},
            #{selfTime, mode=IN, jdbcType=VARCHAR},
            #{manTime, mode=IN, jdbcType=VARCHAR},
            #{otTime, mode=IN, jdbcType=VARCHAR},
            #{okQty, mode=IN, jdbcType=VARCHAR},
            #{ngQty, mode=IN, jdbcType=VARCHAR},
            #{multiStartCnt, mode=IN, jdbcType=VARCHAR},
            #{inputFlag, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

</mapper>
