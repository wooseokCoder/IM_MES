<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
    LSE_MACHINE QUERY 매퍼
    설비 마스터 조회: QUERY3, QUERY7
    작성자: 송우석
    작성일: 2026-02-11
    수정일: 2026-02-23 (STD04A용 QUERY3 추가)
-->
<mapper namespace="com.wsc.imes.std.LSE_MACHINE_QUERY">

    <!-- 설비 목록 조회 (STD04A 메인 그리드용) -->
    <select id="LSE_MACHINE_QUERY3" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_lse_machine_query3(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{mcLike, mode=IN, jdbcType=VARCHAR},
            #{mcModelLike, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 설비 목록 조회 (조건부 필터) -->
    <select id="LSE_MACHINE_QUERY7" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_lse_machine_query7(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{mcCode, mode=IN, jdbcType=VARCHAR},
            #{mcGroup, mode=IN, jdbcType=VARCHAR},
            #{mcOs, mode=IN, jdbcType=VARCHAR},
            #{mcMgtFlag, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=VARCHAR}
        )}
    </select>
    
    <!-- 설비 목록 조회 (ORD06A 자원 콤보용, MC_CODE/MC_NAME/MC_DISP) -->
    <select id="LSE_MACHINE_QUERY1" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_lse_machine_query1(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{mcGroup, mode=IN, jdbcType=VARCHAR},
            #{dataFlag, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!--
    ============================================================
    LSE_MACHINE_QUERY2: 설비 검색 (팝업용)
    원본: LSE_MACHINE_QUERY.LSE_MACHINE_QUERY2()
    용도: ChangeCellMc (POP30B 작업장 선택 팝업)
    추가일: 2026-03-18
    ============================================================
    -->
    <select id="LSE_MACHINE_QUERY2" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_imes_lse_machine_query2(
            #{gsPltCode, mode=IN, jdbcType=VARCHAR},
            #{mcGroup, mode=IN, jdbcType=VARCHAR},
            #{mcLike, mode=IN, jdbcType=VARCHAR},
            #{isSap, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
