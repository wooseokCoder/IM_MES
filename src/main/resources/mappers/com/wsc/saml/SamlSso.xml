<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.saml.SamlSso">

    <select id="selectSalesforceInfo" resultType="record" parameterType="params">
		CALL sp_select_sfdc_sso_info(#{sysId},#{gsUserId})
    </select>
    <select id="selectSfdcInfo" resultType="record" parameterType="params">
		CALL sp_select_sfdc_info(#{sysId},#{gsUserId})
    </select>
    
</mapper>

