<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.user2.User2">

    <!-- ========================================================== -->
    <!--   User2.xml - 프로시저 호출 방식으로 전환                    -->
    <!--   생성일: 2026-01-15                                        -->
    <!-- ========================================================== -->

    <!-- [search] 사용자 목록 조회 (페이징 및 동적 검색) -->
    <select id="search" statementType="CALLABLE" parameterType="params" resultType="record">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        <bind name="userIdListStr" value="userIdList != null and userIdList.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@joinQuoted(',', userIdList) : null" />
        {CALL sp_user2_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{userIdListStr, mode=IN, jdbcType=VARCHAR},
            #{userName, mode=IN, jdbcType=VARCHAR},
            #{userType, mode=IN, jdbcType=VARCHAR},
            #{menuSet, mode=IN, jdbcType=VARCHAR},
            #{menuType, mode=IN, jdbcType=VARCHAR},
            #{orgAuthCode, mode=IN, jdbcType=VARCHAR},
            #{spcAuthCode, mode=IN, jdbcType=VARCHAR},
            #{comCode, mode=IN, jdbcType=VARCHAR},
            #{comName, mode=IN, jdbcType=VARCHAR},
            #{emplNo, mode=IN, jdbcType=VARCHAR},
            #{deptCode, mode=IN, jdbcType=VARCHAR},
            #{deptName, mode=IN, jdbcType=VARCHAR},
            #{upprDeptCode, mode=IN, jdbcType=VARCHAR},
            #{userTel, mode=IN, jdbcType=VARCHAR},
            #{userHp, mode=IN, jdbcType=VARCHAR},
            #{userMail, mode=IN, jdbcType=VARCHAR},
            #{userRemk, mode=IN, jdbcType=LONGVARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER},
            #{sortStr, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchCount] 사용자 카운트 -->
    <select id="searchCount" statementType="CALLABLE" parameterType="params" resultType="int">
        <bind name="userIdListStr" value="userIdList != null and userIdList.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@joinQuoted(',', userIdList) : null" />
        {CALL sp_user2_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{userIdListStr, mode=IN, jdbcType=VARCHAR},
            #{userName, mode=IN, jdbcType=VARCHAR},
            #{userType, mode=IN, jdbcType=VARCHAR},
            #{menuSet, mode=IN, jdbcType=VARCHAR},
            #{menuType, mode=IN, jdbcType=VARCHAR},
            #{orgAuthCode, mode=IN, jdbcType=VARCHAR},
            #{spcAuthCode, mode=IN, jdbcType=VARCHAR},
            #{comCode, mode=IN, jdbcType=VARCHAR},
            #{comName, mode=IN, jdbcType=VARCHAR},
            #{emplNo, mode=IN, jdbcType=VARCHAR},
            #{deptCode, mode=IN, jdbcType=VARCHAR},
            #{deptName, mode=IN, jdbcType=VARCHAR},
            #{upprDeptCode, mode=IN, jdbcType=VARCHAR},
            #{userTel, mode=IN, jdbcType=VARCHAR},
            #{userHp, mode=IN, jdbcType=VARCHAR},
            #{userMail, mode=IN, jdbcType=VARCHAR},
            #{userRemk, mode=IN, jdbcType=LONGVARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [select] 사용자 상세 조회 -->
    <select id="select" statementType="CALLABLE" parameterType="params" resultType="user">
        {CALL sp_user2_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insert] 사용자 등록 -->
    <select id="insert" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{userName, mode=IN, jdbcType=VARCHAR},
            #{userType, mode=IN, jdbcType=VARCHAR},
            #{menuSet, mode=IN, jdbcType=VARCHAR},
            #{menuType, mode=IN, jdbcType=VARCHAR},
            #{mobileType, mode=IN, jdbcType=VARCHAR},
            #{orgAuthCode, mode=IN, jdbcType=VARCHAR},
            #{dashType, mode=IN, jdbcType=VARCHAR},
            #{spcAuthCode, mode=IN, jdbcType=VARCHAR},
            #{applList, mode=IN, jdbcType=LONGVARCHAR},
            #{comCode, mode=IN, jdbcType=VARCHAR},
            #{comName, mode=IN, jdbcType=VARCHAR},
            #{emplNo, mode=IN, jdbcType=VARCHAR},
            #{deptCode, mode=IN, jdbcType=VARCHAR},
            #{upprDeptCode, mode=IN, jdbcType=VARCHAR},
            #{userTel, mode=IN, jdbcType=VARCHAR},
            #{userHp, mode=IN, jdbcType=VARCHAR},
            #{userMail, mode=IN, jdbcType=VARCHAR},
            #{saleGrup, mode=IN, jdbcType=VARCHAR},
            #{regnMagr, mode=IN, jdbcType=VARCHAR},
            #{regnGb, mode=IN, jdbcType=VARCHAR},
            #{shipLoc, mode=IN, jdbcType=VARCHAR},
            #{idLspo, mode=IN, jdbcType=VARCHAR},
            #{idLws, mode=IN, jdbcType=VARCHAR},
            #{idMerc, mode=IN, jdbcType=VARCHAR},
            #{idServ, mode=IN, jdbcType=VARCHAR},
            #{idWgbc, mode=IN, jdbcType=VARCHAR},
            #{idMts, mode=IN, jdbcType=VARCHAR},
            #{idAcb, mode=IN, jdbcType=VARCHAR},
            #{idSproutLoud, mode=IN, jdbcType=VARCHAR},
            #{idSfdc, mode=IN, jdbcType=VARCHAR},
            #{createMailFlag, mode=IN, jdbcType=VARCHAR},
            #{reviewMailFlag, mode=IN, jdbcType=VARCHAR},
            #{confMailFlag, mode=IN, jdbcType=VARCHAR},
            #{readyMailFlag, mode=IN, jdbcType=VARCHAR},
            #{shipMailFlag, mode=IN, jdbcType=VARCHAR},
            #{compMailFlag, mode=IN, jdbcType=VARCHAR},
            #{returnMailFlag, mode=IN, jdbcType=VARCHAR},
            #{sfdcFlag, mode=IN, jdbcType=VARCHAR},
            #{notiAllreadYn, mode=IN, jdbcType=VARCHAR},
            #{SMTP_MAIL, mode=IN, jdbcType=VARCHAR},
            #{SMTP_PW, mode=IN, jdbcType=VARCHAR},
            #{userRemk, mode=IN, jdbcType=LONGVARCHAR},
            #{ifMcCode, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [update] 사용자 수정 -->
    <select id="update" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{userName, mode=IN, jdbcType=VARCHAR},
            #{userType, mode=IN, jdbcType=VARCHAR},
            #{menuSet, mode=IN, jdbcType=VARCHAR},
            #{menuType, mode=IN, jdbcType=VARCHAR},
            #{dashType, mode=IN, jdbcType=VARCHAR},
            #{mobileType, mode=IN, jdbcType=VARCHAR},
            #{orgAuthCode, mode=IN, jdbcType=VARCHAR},
            #{spcAuthCode, mode=IN, jdbcType=VARCHAR},
            #{applList, mode=IN, jdbcType=LONGVARCHAR},
            #{comCode, mode=IN, jdbcType=VARCHAR},
            #{comName, mode=IN, jdbcType=VARCHAR},
            #{emplNo, mode=IN, jdbcType=VARCHAR},
            #{deptCode, mode=IN, jdbcType=VARCHAR},
            #{upprDeptCode, mode=IN, jdbcType=VARCHAR},
            #{userTel, mode=IN, jdbcType=VARCHAR},
            #{userHp, mode=IN, jdbcType=VARCHAR},
            #{userMail, mode=IN, jdbcType=VARCHAR},
            #{saleGrup, mode=IN, jdbcType=VARCHAR},
            #{regnMagr, mode=IN, jdbcType=VARCHAR},
            #{regnGb, mode=IN, jdbcType=VARCHAR},
            #{shipLoc, mode=IN, jdbcType=VARCHAR},
            #{idLspo, mode=IN, jdbcType=VARCHAR},
            #{idLws, mode=IN, jdbcType=VARCHAR},
            #{idMerc, mode=IN, jdbcType=VARCHAR},
            #{idServ, mode=IN, jdbcType=VARCHAR},
            #{idWgbc, mode=IN, jdbcType=VARCHAR},
            #{idMts, mode=IN, jdbcType=VARCHAR},
            #{idAcb, mode=IN, jdbcType=VARCHAR},
            #{idSproutLoud, mode=IN, jdbcType=VARCHAR},
            #{idSfdc, mode=IN, jdbcType=VARCHAR},
            #{createMailFlag, mode=IN, jdbcType=VARCHAR},
            #{reviewMailFlag, mode=IN, jdbcType=VARCHAR},
            #{confMailFlag, mode=IN, jdbcType=VARCHAR},
            #{readyMailFlag, mode=IN, jdbcType=VARCHAR},
            #{shipMailFlag, mode=IN, jdbcType=VARCHAR},
            #{compMailFlag, mode=IN, jdbcType=VARCHAR},
            #{returnMailFlag, mode=IN, jdbcType=VARCHAR},
            #{sfdcFlag, mode=IN, jdbcType=VARCHAR},
            #{notiAllreadYn, mode=IN, jdbcType=VARCHAR},
            #{smtpId, mode=IN, jdbcType=VARCHAR},
            #{smtpPw, mode=IN, jdbcType=VARCHAR},
            #{userRemk, mode=IN, jdbcType=LONGVARCHAR},
            #{ifMcCode, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [delete] 사용자 삭제 -->
    <select id="delete" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [checkPassword] 비밀번호 인증 체크 -->
    <select id="checkPassword" statementType="CALLABLE" parameterType="params" resultType="string">
        {CALL sp_user2_check_password(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{userPwd, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [updatePassword] 비밀번호 변경 -->
    <select id="updatePassword" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_update_password(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{userPwd, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [updateFailure] 로그인 실패 카운트 증가 -->
    <select id="updateFailure" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_update_failure(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [updateSuccess] 로그인 성공 처리 -->
    <select id="updateSuccess" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_update_success(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectUserType] 사용자 타입 목록 조회 -->
    <select id="selectUserType" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_select_user_type(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectUserBm] 서비스 사용자 BM 목록 - 기존 프로시저 호출 유지 -->
    <select id="selectUserBm" resultType="record" parameterType="params">
        CALL sp_get_service_user_list(#{sysId})
    </select>

    <!-- [selectCodeName] 코드명 조회 -->
    <select id="selectCodeName" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_select_code_name(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeGrup, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectDealerCdf] 딜러 CDF 조회 -->
    <select id="selectDealerCdf" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_select_dealer_cdf(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{DEAL_CODE, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectDealerInfo] 딜러 정보 조회 -->
    <select id="selectDealerInfo" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_select_dealer_info(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [dashBoardCurrUnit] 대시보드 통화단위 조회 -->
    <select id="dashBoardCurrUnit" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_dashboard_curr_unit(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [saveDealer] 딜러 등록 -->
    <select id="saveDealer" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_save_dealer(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{userName, mode=IN, jdbcType=VARCHAR},
            #{userType, mode=IN, jdbcType=VARCHAR},
            #{respBm, mode=IN, jdbcType=VARCHAR},
            #{headDeal, mode=IN, jdbcType=VARCHAR},
            #{wareHous, mode=IN, jdbcType=VARCHAR},
            #{shipLoc, mode=IN, jdbcType=VARCHAR},
            #{saleGrup, mode=IN, jdbcType=VARCHAR},
            #{telNo, mode=IN, jdbcType=VARCHAR},
            #{faxNo, mode=IN, jdbcType=VARCHAR},
            #{moNo, mode=IN, jdbcType=VARCHAR},
            #{email, mode=IN, jdbcType=VARCHAR},
            #{emailList, mode=IN, jdbcType=LONGVARCHAR},
            #{emailRecv, mode=IN, jdbcType=LONGVARCHAR},
            #{payType, mode=IN, jdbcType=VARCHAR},
            #{addrStr, mode=IN, jdbcType=VARCHAR},
            #{addrBox, mode=IN, jdbcType=VARCHAR},
            #{addrCity, mode=IN, jdbcType=VARCHAR},
            #{addrCnty, mode=IN, jdbcType=VARCHAR},
            #{addrRegn, mode=IN, jdbcType=VARCHAR},
            #{transZone, mode=IN, jdbcType=VARCHAR},
            #{postCode, mode=IN, jdbcType=VARCHAR},
            #{currUnit, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [saveDealerUser] 딜러 사용자 등록 -->
    <select id="saveDealerUser" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_save_dealer_user(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{userName, mode=IN, jdbcType=VARCHAR},
            #{saleGrup, mode=IN, jdbcType=VARCHAR},
            #{wareHous, mode=IN, jdbcType=VARCHAR},
            #{telNo, mode=IN, jdbcType=VARCHAR},
            #{email, mode=IN, jdbcType=VARCHAR},
            #{emailList, mode=IN, jdbcType=LONGVARCHAR},
            #{idLspo, mode=IN, jdbcType=VARCHAR},
            #{idLws, mode=IN, jdbcType=VARCHAR},
            #{idMerc, mode=IN, jdbcType=VARCHAR},
            #{idServ, mode=IN, jdbcType=VARCHAR},
            #{idWgbc, mode=IN, jdbcType=VARCHAR},
            #{idMts, mode=IN, jdbcType=VARCHAR},
            #{idAcb, mode=IN, jdbcType=VARCHAR},
            #{idSproutLoud, mode=IN, jdbcType=VARCHAR},
            #{idSfdc, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [updateDealer] 딜러 수정 -->
    <select id="updateDealer" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_update_dealer(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{userName, mode=IN, jdbcType=VARCHAR},
            #{headDeal, mode=IN, jdbcType=VARCHAR},
            #{respBm, mode=IN, jdbcType=VARCHAR},
            #{telNo, mode=IN, jdbcType=VARCHAR},
            #{faxNo, mode=IN, jdbcType=VARCHAR},
            #{moNo, mode=IN, jdbcType=VARCHAR},
            #{email, mode=IN, jdbcType=VARCHAR},
            #{emailList, mode=IN, jdbcType=LONGVARCHAR},
            #{emailRecv, mode=IN, jdbcType=LONGVARCHAR},
            #{payType, mode=IN, jdbcType=VARCHAR},
            #{currUnit, mode=IN, jdbcType=VARCHAR},
            #{addrStr, mode=IN, jdbcType=VARCHAR},
            #{addrBox, mode=IN, jdbcType=VARCHAR},
            #{addrCity, mode=IN, jdbcType=VARCHAR},
            #{addrCnty, mode=IN, jdbcType=VARCHAR},
            #{addrRegn, mode=IN, jdbcType=VARCHAR},
            #{transZone, mode=IN, jdbcType=VARCHAR},
            #{postCode, mode=IN, jdbcType=VARCHAR},
            #{saleGrup, mode=IN, jdbcType=VARCHAR},
            #{wareHous, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [updateDealerUser] 딜러 사용자 수정 -->
    <select id="updateDealerUser" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_update_dealer_user(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{saleGrup, mode=IN, jdbcType=VARCHAR},
            #{wareHous, mode=IN, jdbcType=VARCHAR},
            #{telNo, mode=IN, jdbcType=VARCHAR},
            #{email, mode=IN, jdbcType=VARCHAR},
            #{idLspo, mode=IN, jdbcType=VARCHAR},
            #{idLws, mode=IN, jdbcType=VARCHAR},
            #{idMerc, mode=IN, jdbcType=VARCHAR},
            #{idServ, mode=IN, jdbcType=VARCHAR},
            #{idWgbc, mode=IN, jdbcType=VARCHAR},
            #{idMts, mode=IN, jdbcType=VARCHAR},
            #{idAcb, mode=IN, jdbcType=VARCHAR},
            #{idSproutLoud, mode=IN, jdbcType=VARCHAR},
            #{idSfdc, mode=IN, jdbcType=VARCHAR},
            #{type, mode=IN, jdbcType=VARCHAR},
            #{FirstName, mode=IN, jdbcType=VARCHAR},
            #{LastName, mode=IN, jdbcType=VARCHAR},
            #{userAddress1, mode=IN, jdbcType=VARCHAR},
            #{userCity, mode=IN, jdbcType=VARCHAR},
            #{userRegnCode, mode=IN, jdbcType=VARCHAR},
            #{userPostCode, mode=IN, jdbcType=VARCHAR},
            #{tpAccountId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [updateDealerSink] 딜러 동기화 수정 -->
    <select id="updateDealerSink" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_update_dealer_sink(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{saleGrup, mode=IN, jdbcType=VARCHAR},
            #{shipLoc, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectBmList] BM 목록 조회 -->
    <select id="selectBmList" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_select_bm_list(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectBmList1] BM 목록 조회 (특정 사용자 제외) -->
    <select id="selectBmList1" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_select_bm_list1(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectHeadDealList] HEAD DEAL 목록 조회 -->
    <select id="selectHeadDealList" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_select_head_deal_list(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectAuto] 자동 조회 (SALE_GRUP, SHIP_LOC) -->
    <select id="selectAuto" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_select_auto(
            #{deal_id, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchType] 사용자 타입 검색 -->
    <select id="searchType" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_user2_search_type(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchApplList] 애플리케이션 목록 검색 - 기존 프로시저 호출 유지 -->
    <select id="searchApplList" resultType="record" parameterType="params">
        CALL sp_get_search_appl_list_code(#{sysId},#{userId})
    </select>

</mapper>
