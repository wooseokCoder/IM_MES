<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.user.BatchWork">

    <!-- ========================================================== -->
    <!--   BatchWork Procedures - Converted to CALL statements      -->
    <!--   Table: SYS_JOB_MAST                                      -->
    <!-- ========================================================== -->

    <!-- ========================================================== -->
    <!--   search - Search batch work list with pagination          -->
    <!-- ========================================================== -->
    <select id="search" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_batchwork_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobGrup, mode=IN, jdbcType=VARCHAR},
            #{jobTerm, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   searchCount - Count batch work records                   -->
    <!-- ========================================================== -->
    <select id="searchCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_batchwork_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobGrup, mode=IN, jdbcType=VARCHAR},
            #{jobTerm, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   select - Select single batch work record by ID           -->
    <!-- ========================================================== -->
    <select id="select" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_batchwork_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   insert - Insert new batch work record                    -->
    <!--   Note: jobId is returned via OUT parameter                -->
    <!-- ========================================================== -->
    <select id="insert" statementType="CALLABLE" parameterType="params">
        {CALL sp_batchwork_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobGrup, mode=IN, jdbcType=VARCHAR},
            #{jobType, mode=IN, jdbcType=VARCHAR},
            #{jobTerm, mode=IN, jdbcType=VARCHAR},
            #{jobTime, mode=IN, jdbcType=VARCHAR},
            #{jobCmd, mode=IN, jdbcType=VARCHAR},
            #{jobDesc, mode=IN, jdbcType=VARCHAR},
            #{errProc, mode=IN, jdbcType=VARCHAR},
            #{jobMng, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{jobRemk, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=OUT, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   update - Update batch work record                        -->
    <!-- ========================================================== -->
    <update id="update" statementType="CALLABLE" parameterType="params">
        {CALL sp_batchwork_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=IN, jdbcType=VARCHAR},
            #{newJobId, mode=IN, jdbcType=VARCHAR},
            #{jobGrup, mode=IN, jdbcType=VARCHAR},
            #{jobType, mode=IN, jdbcType=VARCHAR},
            #{jobTerm, mode=IN, jdbcType=VARCHAR},
            #{jobTime, mode=IN, jdbcType=VARCHAR},
            #{jobCmd, mode=IN, jdbcType=VARCHAR},
            #{jobDesc, mode=IN, jdbcType=VARCHAR},
            #{errProc, mode=IN, jdbcType=VARCHAR},
            #{jobMng, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{jobRemk, mode=IN, jdbcType=VARCHAR},
            #{regiId, mode=IN, jdbcType=VARCHAR},
            #{regiDate, mode=IN, jdbcType=TIMESTAMP},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

</mapper>
