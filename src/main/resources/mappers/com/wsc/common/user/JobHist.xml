<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.user.JobHist">

    <!-- ========================================================== -->
    <!--   JobHist Management - Stored Procedure Calls              -->
    <!-- ========================================================== -->

    <!-- search: 작업 이력 목록 조회 (페이징) -->
    <select id="search" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_jobhist_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=IN, jdbcType=VARCHAR},
            #{jobDesc, mode=IN, jdbcType=VARCHAR},
            #{jobRslt, mode=IN, jdbcType=VARCHAR},
            #{bgnDate, mode=IN, jdbcType=VARCHAR},
            #{endDate, mode=IN, jdbcType=VARCHAR},
            #{rsltDesc, mode=IN, jdbcType=VARCHAR},
            #{jobFile, mode=IN, jdbcType=VARCHAR},
            #{accTimeBgn, mode=IN, jdbcType=VARCHAR},
            #{accTimeEnd, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER},
            #{sortStr, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- searchCount: 작업 이력 건수 조회 -->
    <select id="searchCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_jobhist_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=IN, jdbcType=VARCHAR},
            #{jobDesc, mode=IN, jdbcType=VARCHAR},
            #{jobRslt, mode=IN, jdbcType=VARCHAR},
            #{bgnDate, mode=IN, jdbcType=VARCHAR},
            #{endDate, mode=IN, jdbcType=VARCHAR},
            #{rsltDesc, mode=IN, jdbcType=VARCHAR},
            #{jobFile, mode=IN, jdbcType=VARCHAR},
            #{accTimeBgn, mode=IN, jdbcType=VARCHAR},
            #{accTimeEnd, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- insert: 작업 이력 등록 -->
    <insert id="insert" statementType="CALLABLE" parameterType="params">
        {CALL sp_jobhist_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=IN, jdbcType=VARCHAR},
            #{regiId, mode=IN, jdbcType=VARCHAR},
            #{chngId, mode=IN, jdbcType=VARCHAR},
            #{jobFile, mode=IN, jdbcType=VARCHAR},
            #{jobRslt, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- delete: 작업 이력 삭제 -->
    <delete id="delete" statementType="CALLABLE" parameterType="params">
        {CALL sp_jobhist_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobId, mode=IN, jdbcType=VARCHAR},
            #{regiId, mode=IN, jdbcType=VARCHAR},
            #{chngId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- selectJobIdList: Job ID 목록 조회 -->
    <select id="selectJobIdList" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_jobhist_select_job_id_list(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- selectJobRsltList: Job 결과 목록 조회 -->
    <select id="selectJobRsltList" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_jobhist_select_job_rslt_list(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- searchJobHistGroup: 작업 이력 그룹 조회 (페이징) -->
    <select id="searchJobHistGroup" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_jobhist_search_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{groupId, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER},
            #{sortStr, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
