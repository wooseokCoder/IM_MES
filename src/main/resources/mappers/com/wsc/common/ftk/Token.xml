<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.ftk.Token">

    <!-- ========================================================== -->
    <!--   1. 토큰 관리 - 프로시저 호출                              -->
    <!-- ========================================================== -->

    <!-- [createToken] 토큰 생성 -->
    <select id="createToken" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL SP_CREATE_TOKEN(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{issuUrl, mode=IN, jdbcType=VARCHAR},
            #{rqstUrl, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectToken] 토큰 번호 조회 (함수 호출) -->
    <select id="selectToken" resultType="string" parameterType="params">
        SELECT FN_MAKE_TOKEN()
    </select>

    <!-- [validateToken] 토큰 검증 -->
    <select id="validateToken" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL SP_VALIDATE_TOKEN(
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{ftokenNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [validateToken2] 토큰 검증 2 -->
    <select id="validateToken2" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL SP_VALIDATE_TOKEN2(
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{ftokenNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   2. PDI 인터페이스 - 프로시저 호출                         -->
    <!-- ========================================================== -->

    <!-- [seriNoUpdate] 시리얼 번호 업데이트 -->
    <select id="seriNoUpdate" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL SP_IF_PDI(
            'SERI_NO',
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{itemCode, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{seriNo, mode=IN, jdbcType=VARCHAR},
            '', '', '', ''
        )}
    </select>

    <!-- [outBoundUpdate] 출고 업데이트 -->
    <select id="outBoundUpdate" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL SP_IF_PDI(
            'OUTBOUND',
            '', '',
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{bolNo, mode=IN, jdbcType=VARCHAR},
            #{actShipDate, mode=IN, jdbcType=VARCHAR},
            '', '', ''
        )}
    </select>

    <!-- [outBoundCancelUpdate] 출고 취소 업데이트 -->
    <select id="outBoundCancelUpdate" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL SP_IF_PDI(
            'OUTBOUND_CNSL',
            '', '',
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{poNo, mode=IN, jdbcType=VARCHAR},
            '', '', '', ''
        )}
    </select>

    <!-- [assyNoUpdate] 조립 번호 업데이트 -->
    <select id="assyNoUpdate" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL SP_IF_PDI(
            'ASSY_NO',
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            '',
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{assyNo, mode=IN, jdbcType=VARCHAR},
            #{assyDate, mode=IN, jdbcType=VARCHAR},
            '', '', ''
        )}
    </select>

    <!-- [ifOrdrUpdate] 주문 인터페이스 업데이트 -->
    <select id="ifOrdrUpdate" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL SP_IF_PDI(
            #{type, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{itemCode, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{var1, mode=IN, jdbcType=VARCHAR},
            #{var2, mode=IN, jdbcType=VARCHAR},
            #{var3, mode=IN, jdbcType=VARCHAR},
            #{var4, mode=IN, jdbcType=VARCHAR},
            #{var5, mode=IN, jdbcType=VARCHAR},
            #{var6, mode=IN, jdbcType=VARCHAR},
            #{var7, mode=IN, jdbcType=VARCHAR},
            #{var8, mode=IN, jdbcType=VARCHAR},
            #{var9, mode=IN, jdbcType=VARCHAR},
            #{var10, mode=IN, jdbcType=VARCHAR},
            #{var11, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [outboundBolImgUpdate] 출고 BOL 이미지 업데이트 -->
    <select id="outboundBolImgUpdate" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL SP_IF_PDI(
            #{type, mode=IN, jdbcType=VARCHAR},
            #{ordrNo, mode=IN, jdbcType=VARCHAR},
            #{itemCode, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{var1, mode=IN, jdbcType=VARCHAR},
            #{var2, mode=IN, jdbcType=VARCHAR},
            #{var3, mode=IN, jdbcType=VARCHAR},
            #{var4, mode=IN, jdbcType=VARCHAR},
            #{var5, mode=IN, jdbcType=VARCHAR},
            #{var6, mode=IN, jdbcType=VARCHAR},
            #{var7, mode=IN, jdbcType=VARCHAR},
            #{var8, mode=IN, jdbcType=VARCHAR},
            #{var9, mode=IN, jdbcType=VARCHAR},
            #{var10, mode=IN, jdbcType=VARCHAR},
            #{var11, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [ifLsas] LSAS 인터페이스 -->
    <select id="ifLsas" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL SP_IF_LSAS(
            #{var1, mode=IN, jdbcType=VARCHAR},
            #{var2, mode=IN, jdbcType=VARCHAR},
            #{var3, mode=IN, jdbcType=VARCHAR},
            #{var4, mode=IN, jdbcType=VARCHAR},
            #{var5, mode=IN, jdbcType=VARCHAR},
            #{var6, mode=IN, jdbcType=VARCHAR},
            #{var7, mode=IN, jdbcType=VARCHAR},
            #{var8, mode=IN, jdbcType=VARCHAR},
            #{var9, mode=IN, jdbcType=VARCHAR},
            #{var10, mode=IN, jdbcType=VARCHAR},
            #{var11, mode=IN, jdbcType=VARCHAR},
            #{var12, mode=IN, jdbcType=VARCHAR},
            #{var13, mode=IN, jdbcType=VARCHAR},
            #{var14, mode=IN, jdbcType=VARCHAR},
            #{var15, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   3. API 임시 로그 관리 (SYS_API_TEMP_LOG) - 프로시저 호출  -->
    <!-- ========================================================== -->

    <!-- [TmpLogInsert] API 임시 로그 등록 -->
    <insert id="TmpLogInsert" statementType="CALLABLE" parameterType="params">
        {CALL sp_token_tmp_log_insert(
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{token, mode=IN, jdbcType=VARCHAR},
            #{ordr_no, mode=IN, jdbcType=VARCHAR},
            #{assy_no, mode=IN, jdbcType=VARCHAR},
            #{type, mode=IN, jdbcType=VARCHAR},
            #{val, mode=IN, jdbcType=VARCHAR},
            #{val2, mode=IN, jdbcType=VARCHAR},
            #{val3, mode=IN, jdbcType=VARCHAR},
            #{val4, mode=IN, jdbcType=VARCHAR},
            #{val5, mode=IN, jdbcType=VARCHAR},
            #{val6, mode=IN, jdbcType=VARCHAR},
            #{val7, mode=IN, jdbcType=VARCHAR},
            #{val8, mode=IN, jdbcType=VARCHAR},
            #{val9, mode=IN, jdbcType=VARCHAR},
            #{val10, mode=IN, jdbcType=VARCHAR},
            #{val11, mode=IN, jdbcType=VARCHAR},
            #{val12, mode=IN, jdbcType=VARCHAR},
            #{val13, mode=IN, jdbcType=VARCHAR},
            #{val14, mode=IN, jdbcType=VARCHAR},
            #{val15, mode=IN, jdbcType=VARCHAR},
            #{val16, mode=IN, jdbcType=VARCHAR},
            #{val17, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [TmpLogUpdate] API 임시 로그 결과 업데이트 -->
    <update id="TmpLogUpdate" statementType="CALLABLE" parameterType="params">
        {CALL sp_token_tmp_log_update(
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{token, mode=IN, jdbcType=VARCHAR},
            #{ret, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- ========================================================== -->
    <!--   4. API 키 조회 (함수 호출)                                -->
    <!-- ========================================================== -->

    <!-- [getApiKey] API 키 조회 -->
    <select id="getApiKey" parameterType="params" resultType="record">
        SELECT FN_API_KEY_GET(#{sysId}) AS API_KEY
    </select>

</mapper>
