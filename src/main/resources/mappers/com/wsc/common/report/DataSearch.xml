<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.report.DataSearch">

    <!-- ========================================================== -->
    <!--   1. 조회 - 프로시저 호출                                   -->
    <!-- ========================================================== -->

    <!-- [search] DATA_OPER_MAST 목록 조회 (페이징 + 동적 정렬) -->
    <select id="search" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_data_search_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{searchEmplName, mode=IN, jdbcType=VARCHAR},
            #{compCode, mode=IN, jdbcType=VARCHAR},
            #{searchjobNo, mode=IN, jdbcType=VARCHAR},
            #{deptCode, mode=IN, jdbcType=VARCHAR},
            #{deptName, mode=IN, jdbcType=VARCHAR},
            #{upprDeptCode, mode=IN, jdbcType=VARCHAR},
            #{emplTel, mode=IN, jdbcType=VARCHAR},
            #{emplHp, mode=IN, jdbcType=VARCHAR},
            #{emplMail, mode=IN, jdbcType=VARCHAR},
            #{emplRemk, mode=IN, jdbcType=VARCHAR},
            #{exitYn, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=INTEGER},
            #{end, mode=IN, jdbcType=INTEGER}
        )}
    </select>

    <!-- [searchCount] DATA_OPER_MAST 카운트 -->
    <select id="searchCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_data_search_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{searchEmplName, mode=IN, jdbcType=VARCHAR},
            #{compCode, mode=IN, jdbcType=VARCHAR},
            #{searchjobNo, mode=IN, jdbcType=VARCHAR},
            #{deptCode, mode=IN, jdbcType=VARCHAR},
            #{deptName, mode=IN, jdbcType=VARCHAR},
            #{upprDeptCode, mode=IN, jdbcType=VARCHAR},
            #{emplTel, mode=IN, jdbcType=VARCHAR},
            #{emplHp, mode=IN, jdbcType=VARCHAR},
            #{emplMail, mode=IN, jdbcType=VARCHAR},
            #{emplRemk, mode=IN, jdbcType=VARCHAR},
            #{exitYn, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchSub] DATA_OPER_DETL 서브 목록 조회 -->
    <select id="searchSub" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_data_search_sub(
            #{d_sysId, mode=IN, jdbcType=VARCHAR},
            #{d_jobNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchSubCount] DATA_OPER_DETL 서브 카운트 -->
    <select id="searchSubCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_data_search_sub_count(
            #{d_sysId, mode=IN, jdbcType=VARCHAR},
            #{d_jobNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchAll] DATA_OPER_MAST 단건 조회 -->
    <select id="searchAll" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_data_search_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobNo, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getjobNo] JOB_NO 생성 -->
    <select id="getjobNo" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_data_search_get_job_no(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   2. CRUD - 프로시저 호출                                   -->
    <!-- ========================================================== -->

    <!-- [insert] DATA_OPER_MAST 등록 -->
    <insert id="insert" statementType="CALLABLE" parameterType="params">
        {CALL sp_data_search_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{emplName, mode=IN, jdbcType=VARCHAR},
            #{emplNameEng, mode=IN, jdbcType=VARCHAR},
            #{emplNameHan, mode=IN, jdbcType=VARCHAR},
            #{sociSecuNo, mode=IN, jdbcType=VARCHAR},
            #{birthDay, mode=IN, jdbcType=VARCHAR},
            #{solLunCal, mode=IN, jdbcType=VARCHAR},
            #{gender, mode=IN, jdbcType=VARCHAR},
            #{joinDay, mode=IN, jdbcType=VARCHAR},
            #{deptCode, mode=IN, jdbcType=VARCHAR},
            #{deptName, mode=IN, jdbcType=VARCHAR},
            #{upprDeptCode, mode=IN, jdbcType=VARCHAR},
            #{position, mode=IN, jdbcType=VARCHAR},
            #{busiCharge, mode=IN, jdbcType=VARCHAR},
            #{address, mode=IN, jdbcType=VARCHAR},
            #{addNo, mode=IN, jdbcType=VARCHAR},
            #{bonAddress, mode=IN, jdbcType=VARCHAR},
            #{bonAddNo, mode=IN, jdbcType=VARCHAR},
            #{emplTel, mode=IN, jdbcType=VARCHAR},
            #{emplHp, mode=IN, jdbcType=VARCHAR},
            #{emplMail, mode=IN, jdbcType=VARCHAR},
            #{bloodType, mode=IN, jdbcType=VARCHAR},
            #{heigth, mode=IN, jdbcType=VARCHAR},
            #{sight, mode=IN, jdbcType=VARCHAR},
            #{hobby, mode=IN, jdbcType=VARCHAR},
            #{religion, mode=IN, jdbcType=VARCHAR},
            #{medicalHistory, mode=IN, jdbcType=VARCHAR},
            #{weigth, mode=IN, jdbcType=VARCHAR},
            #{garden, mode=IN, jdbcType=VARCHAR},
            #{estate, mode=IN, jdbcType=VARCHAR},
            #{dwelling, mode=IN, jdbcType=VARCHAR},
            #{site, mode=IN, jdbcType=VARCHAR},
            #{photo, mode=IN, jdbcType=VARCHAR},
            #{emplRemk, mode=IN, jdbcType=VARCHAR},
            #{exitYn, mode=IN, jdbcType=VARCHAR},
            #{exitDate, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [update] DATA_OPER_MAST 수정 -->
    <update id="update" statementType="CALLABLE" parameterType="params">
        {CALL sp_data_search_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobNo, mode=IN, jdbcType=VARCHAR},
            #{emplName, mode=IN, jdbcType=VARCHAR},
            #{emplNameEng, mode=IN, jdbcType=VARCHAR},
            #{emplNameHan, mode=IN, jdbcType=VARCHAR},
            #{sociSecuNo, mode=IN, jdbcType=VARCHAR},
            #{birthDay, mode=IN, jdbcType=VARCHAR},
            #{solLunCal, mode=IN, jdbcType=VARCHAR},
            #{gender, mode=IN, jdbcType=VARCHAR},
            #{joinDay, mode=IN, jdbcType=VARCHAR},
            #{deptCode, mode=IN, jdbcType=VARCHAR},
            #{deptName, mode=IN, jdbcType=VARCHAR},
            #{upprDeptCode, mode=IN, jdbcType=VARCHAR},
            #{position, mode=IN, jdbcType=VARCHAR},
            #{busiCharge, mode=IN, jdbcType=VARCHAR},
            #{address, mode=IN, jdbcType=VARCHAR},
            #{addNo, mode=IN, jdbcType=VARCHAR},
            #{bonAddress, mode=IN, jdbcType=VARCHAR},
            #{bonAddNo, mode=IN, jdbcType=VARCHAR},
            #{emplTel, mode=IN, jdbcType=VARCHAR},
            #{emplHp, mode=IN, jdbcType=VARCHAR},
            #{emplMail, mode=IN, jdbcType=VARCHAR},
            #{bloodType, mode=IN, jdbcType=VARCHAR},
            #{heigth, mode=IN, jdbcType=VARCHAR},
            #{sight, mode=IN, jdbcType=VARCHAR},
            #{hobby, mode=IN, jdbcType=VARCHAR},
            #{religion, mode=IN, jdbcType=VARCHAR},
            #{medicalHistory, mode=IN, jdbcType=VARCHAR},
            #{weigth, mode=IN, jdbcType=VARCHAR},
            #{garden, mode=IN, jdbcType=VARCHAR},
            #{estate, mode=IN, jdbcType=VARCHAR},
            #{dwelling, mode=IN, jdbcType=VARCHAR},
            #{site, mode=IN, jdbcType=VARCHAR},
            #{photo, mode=IN, jdbcType=VARCHAR},
            #{emplRemk, mode=IN, jdbcType=VARCHAR},
            #{exitYn, mode=IN, jdbcType=VARCHAR},
            #{exitDate, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [delete] DATA_OPER_MAST 삭제 -->
    <delete id="delete" statementType="CALLABLE" parameterType="params">
        {CALL sp_data_search_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   3. 기타 조회 - 프로시저 호출                              -->
    <!-- ========================================================== -->

    <!-- [DataSearchgetFieldTitle] 필드 타이틀 조회 (동적 정렬) -->
    <select id="DataSearchgetFieldTitle" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_data_search_get_field_title(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{searchEmplName, mode=IN, jdbcType=VARCHAR},
            #{compCode, mode=IN, jdbcType=VARCHAR},
            #{searchjobNo, mode=IN, jdbcType=VARCHAR},
            #{deptCode, mode=IN, jdbcType=VARCHAR},
            #{deptName, mode=IN, jdbcType=VARCHAR},
            #{upprDeptCode, mode=IN, jdbcType=VARCHAR},
            #{emplTel, mode=IN, jdbcType=VARCHAR},
            #{emplHp, mode=IN, jdbcType=VARCHAR},
            #{emplMail, mode=IN, jdbcType=VARCHAR},
            #{emplRemk, mode=IN, jdbcType=VARCHAR},
            #{exitYn, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getSqlData] SQL 데이터 조회 -->
    <select id="getSqlData" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL sp_data_search_get_sql_data(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getSqlParameter] SQL 파라미터 조회 -->
    <select id="getSqlParameter" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_data_search_get_sql_parameter(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{jobNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [getGroupId] 그룹 ID 조회 -->
    <select id="getGroupId" statementType="CALLABLE" resultType="string" parameterType="params">
        {CALL sp_data_search_get_group_id(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchJobType] Job 타입 조회 -->
    <select id="searchJobType" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_data_search_job_type(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
