<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.loader.Loader">

    <!-- [search] 로더 폼 목록 조회 (페이징) -->
    <select id="search" statementType="CALLABLE" parameterType="map" resultType="loaderForm">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_loader_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{formName, mode=IN, jdbcType=VARCHAR},
            #{formDesc, mode=IN, jdbcType=VARCHAR},
            #{titleNo, mode=IN, jdbcType=VARCHAR},
            #{startNo, mode=IN, jdbcType=VARCHAR},
            #{pivotYn, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchCount] 로더 폼 카운트 -->
    <select id="searchCount" statementType="CALLABLE" parameterType="map" resultType="int">
        {CALL sp_loader_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{formName, mode=IN, jdbcType=VARCHAR},
            #{formDesc, mode=IN, jdbcType=VARCHAR},
            #{titleNo, mode=IN, jdbcType=VARCHAR},
            #{startNo, mode=IN, jdbcType=VARCHAR},
            #{pivotYn, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [select] 로더 폼 단건 조회 -->
    <select id="select" statementType="CALLABLE" parameterType="map" resultType="loaderForm">
        {CALL sp_loader_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insert] 로더 폼 등록 -->
    <insert id="insert" statementType="CALLABLE" parameterType="map">
        {CALL sp_loader_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{formName, mode=IN, jdbcType=VARCHAR},
            #{formDesc, mode=IN, jdbcType=VARCHAR},
            #{titleNo, mode=IN, jdbcType=VARCHAR},
            #{startNo, mode=IN, jdbcType=VARCHAR},
            #{pivotYn, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [update] 로더 폼 수정 -->
    <update id="update" statementType="CALLABLE" parameterType="map">
        {CALL sp_loader_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{formName, mode=IN, jdbcType=VARCHAR},
            #{formDesc, mode=IN, jdbcType=VARCHAR},
            #{titleNo, mode=IN, jdbcType=VARCHAR},
            #{startNo, mode=IN, jdbcType=VARCHAR},
            #{pivotYn, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [delete] 로더 폼 삭제 -->
    <delete id="delete" statementType="CALLABLE" parameterType="map">
        {CALL sp_loader_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   Excel Field Information (Item)                           -->
    <!-- ========================================================== -->

    <!-- [searchItem] 로더 아이템 목록 조회 (페이징) -->
    <select id="searchItem" statementType="CALLABLE" parameterType="map" resultType="loaderItem">
        {CALL sp_loader_search_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{itemCode, mode=IN, jdbcType=VARCHAR},
            #{itemName, mode=IN, jdbcType=VARCHAR},
            #{itemType, mode=IN, jdbcType=VARCHAR},
            #{itemDesc, mode=IN, jdbcType=VARCHAR},
            #{itemDef, mode=IN, jdbcType=VARCHAR},
            #{itemSeq, mode=IN, jdbcType=VARCHAR},
            #{exclCode, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchItemCount] 로더 아이템 카운트 -->
    <select id="searchItemCount" statementType="CALLABLE" parameterType="map" resultType="int">
        {CALL sp_loader_search_item_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{itemCode, mode=IN, jdbcType=VARCHAR},
            #{itemName, mode=IN, jdbcType=VARCHAR},
            #{itemType, mode=IN, jdbcType=VARCHAR},
            #{itemDesc, mode=IN, jdbcType=VARCHAR},
            #{itemDef, mode=IN, jdbcType=VARCHAR},
            #{itemSeq, mode=IN, jdbcType=VARCHAR},
            #{exclCode, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectItem] 로더 아이템 단건 조회 -->
    <select id="selectItem" statementType="CALLABLE" parameterType="map" resultType="loaderItem">
        {CALL sp_loader_select_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{itemCode, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insertItem] 로더 아이템 등록 -->
    <insert id="insertItem" statementType="CALLABLE" parameterType="map">
        {CALL sp_loader_insert_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{itemCode, mode=IN, jdbcType=VARCHAR},
            #{itemName, mode=IN, jdbcType=VARCHAR},
            #{itemType, mode=IN, jdbcType=VARCHAR},
            #{itemDesc, mode=IN, jdbcType=VARCHAR},
            #{itemDef, mode=IN, jdbcType=VARCHAR},
            #{itemSeq, mode=IN, jdbcType=VARCHAR},
            #{exclCode, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [updateItem] 로더 아이템 수정 -->
    <update id="updateItem" statementType="CALLABLE" parameterType="map">
        {CALL sp_loader_update_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{itemCode, mode=IN, jdbcType=VARCHAR},
            #{itemName, mode=IN, jdbcType=VARCHAR},
            #{itemType, mode=IN, jdbcType=VARCHAR},
            #{itemDesc, mode=IN, jdbcType=VARCHAR},
            #{itemDef, mode=IN, jdbcType=VARCHAR},
            #{itemSeq, mode=IN, jdbcType=VARCHAR},
            #{exclCode, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [deleteItem] 로더 아이템 삭제 -->
    <delete id="deleteItem" statementType="CALLABLE" parameterType="map">
        {CALL sp_loader_delete_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR},
            #{itemCode, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [deleteItemAll] 로더 아이템 전체 삭제 -->
    <delete id="deleteItemAll" statementType="CALLABLE" parameterType="map">
        {CALL sp_loader_delete_item_all(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{exclGrup, mode=IN, jdbcType=VARCHAR},
            #{formCode, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [searchCombo] 콤보형 데이터 검색 -->
    <select id="searchCombo" statementType="CALLABLE" parameterType="map" resultType="record">
        {CALL sp_loader_search_combo(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeGrup, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
