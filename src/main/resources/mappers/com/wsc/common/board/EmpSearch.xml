<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.board.EmpSearch">

    <!-- ========================================================== -->
    <!--   사원 검색 (TSTD_EMPLOYEE) - acEmpForm 팝업용              -->
    <!-- ========================================================== -->

    <!-- [search] 사원 목록 조회 -->
    <select id="search" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_emp_search(
            #{pltCode, mode=IN, jdbcType=VARCHAR},
            #{empLike, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
