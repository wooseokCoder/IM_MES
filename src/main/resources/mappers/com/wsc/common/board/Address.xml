<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.board.Address">

    <!-- ========================================================== -->
    <!--   주소록 관리 (SYS_BORD_ADDR) - 프로시저 호출               -->
    <!-- ========================================================== -->

    <!-- [search] 주소록 목록 조회 (페이징, 동적 정렬) -->
    <select id="search" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_address_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserName, mode=IN, jdbcType=VARCHAR},
            #{tgtUserRmk, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrCode, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrName, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchCount] 주소록 카운트 -->
    <select id="searchCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_address_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserName, mode=IN, jdbcType=VARCHAR},
            #{tgtUserRmk, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrCode, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrName, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [select] 주소록 단건 조회 -->
    <select id="select" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_address_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insert] 주소록 등록 -->
    <insert id="insert" statementType="CALLABLE" parameterType="params">
        {CALL sp_address_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserName, mode=IN, jdbcType=VARCHAR},
            #{tgtUserRmk, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrCode, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrName, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [update] 주소록 수정 -->
    <update id="update" statementType="CALLABLE" parameterType="params">
        {CALL sp_address_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserName, mode=IN, jdbcType=VARCHAR},
            #{tgtUserRmk, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrCode, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrName, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [delete] 주소록 삭제 -->
    <delete id="delete" statementType="CALLABLE" parameterType="params">
        {CALL sp_address_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   그룹 관리 (SYS_BORD_GRUP) - 프로시저 호출                 -->
    <!-- ========================================================== -->

    <!-- [searchGroup] 그룹 목록 조회 (페이징, 동적 정렬) -->
    <select id="searchGroup" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_address_search_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupName, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchGroupCount] 그룹 카운트 -->
    <select id="searchGroupCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_address_search_group_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupName, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectGroup] 그룹 단건 조회 -->
    <select id="selectGroup" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_address_select_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insertGroup] 그룹 등록 (PK 자동 생성 후 반환) -->
    <select id="insertGroup" statementType="CALLABLE" parameterType="params" resultType="record">
        {CALL sp_address_insert_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{bordGrup, mode=IN, jdbcType=VARCHAR},
            #{userId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupName, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupRmk, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [updateGroup] 그룹 수정 -->
    <update id="updateGroup" statementType="CALLABLE" parameterType="params">
        {CALL sp_address_update_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupName, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupRmk, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [deleteGroup] 그룹 삭제 -->
    <delete id="deleteGroup" statementType="CALLABLE" parameterType="params">
        {CALL sp_address_delete_group(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   그룹 아이템 관리 (SYS_BORD_ITEM) - 프로시저 호출          -->
    <!-- ========================================================== -->

    <!-- [searchGroupItem] 그룹 아이템 목록 조회 (페이징, 동적 정렬) -->
    <select id="searchGroupItem" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_address_search_group_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchGroupItemCount] 그룹 아이템 카운트 -->
    <select id="searchGroupItemCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_address_search_group_item_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [selectGroupItem] 그룹 아이템 단건 조회 -->
    <select id="selectGroupItem" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_address_select_group_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insertGroupItem] 그룹 아이템 등록 -->
    <insert id="insertGroupItem" statementType="CALLABLE" parameterType="params">
        {CALL sp_address_insert_group_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserName, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrCode, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrName, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- [updateGroupItem] 그룹 아이템 수정 -->
    <update id="updateGroupItem" statementType="CALLABLE" parameterType="params">
        {CALL sp_address_update_group_item(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserId, mode=IN, jdbcType=VARCHAR},
            #{tgtUserName, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrCode, mode=IN, jdbcType=VARCHAR},
            #{tgtVndrName, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [deleteGroupItemAll] 그룹 아이템 전체 삭제 -->
    <delete id="deleteGroupItemAll" statementType="CALLABLE" parameterType="params">
        {CALL sp_address_delete_group_item_all(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{tgtGrupId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

</mapper>
