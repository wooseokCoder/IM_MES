<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.file.File">

    <!-- [search] 파일 목록 조회 (페이징 없음) -->
    <select id="search" statementType="CALLABLE" parameterType="params" resultType="file">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_file_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{atchGrup, mode=IN, jdbcType=VARCHAR},
            #{atchNo, mode=IN, jdbcType=VARCHAR},
            #{fileNo, mode=IN, jdbcType=VARCHAR},
            #{fileName, mode=IN, jdbcType=VARCHAR},
            #{saveName, mode=IN, jdbcType=VARCHAR},
            #{filePath, mode=IN, jdbcType=VARCHAR},
            #{fileType, mode=IN, jdbcType=VARCHAR},
            #{fileSize, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [searchCount] 파일 카운트 -->
    <select id="searchCount" statementType="CALLABLE" parameterType="params" resultType="int">
        {CALL sp_file_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{atchGrup, mode=IN, jdbcType=VARCHAR},
            #{atchNo, mode=IN, jdbcType=VARCHAR},
            #{fileNo, mode=IN, jdbcType=VARCHAR},
            #{fileName, mode=IN, jdbcType=VARCHAR},
            #{saveName, mode=IN, jdbcType=VARCHAR},
            #{filePath, mode=IN, jdbcType=VARCHAR},
            #{fileType, mode=IN, jdbcType=VARCHAR},
            #{fileSize, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [select] 파일 단건 조회 -->
    <select id="select" statementType="CALLABLE" parameterType="params" resultType="file">
        {CALL sp_file_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{atchGrup, mode=IN, jdbcType=VARCHAR},
            #{atchNo, mode=IN, jdbcType=VARCHAR},
            #{fileNo, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [insert] 파일 등록 - fileNo 반환 -->
    <select id="insert" statementType="CALLABLE" parameterType="file" resultType="file">
        {CALL sp_file_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{atchGrup, mode=IN, jdbcType=VARCHAR},
            #{atchNo, mode=IN, jdbcType=VARCHAR},
            #{fileName, mode=IN, jdbcType=VARCHAR},
            #{saveName, mode=IN, jdbcType=VARCHAR},
            #{filePath, mode=IN, jdbcType=VARCHAR},
            #{fileType, mode=IN, jdbcType=VARCHAR},
            #{fileSize, mode=IN, jdbcType=VARCHAR},
            #{comment, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- [delete] 파일 삭제 -->
    <delete id="delete" statementType="CALLABLE" parameterType="file">
        {CALL sp_file_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{atchGrup, mode=IN, jdbcType=VARCHAR},
            #{atchNo, mode=IN, jdbcType=VARCHAR},
            #{fileNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [deleteAll] 파일 전체 삭제 -->
    <delete id="deleteAll" statementType="CALLABLE" parameterType="file">
        {CALL sp_file_delete_all(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{atchGrup, mode=IN, jdbcType=VARCHAR},
            #{atchNo, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- [updateThumbNail] 썸네일 설정 -->
    <update id="updateThumbNail" statementType="CALLABLE" parameterType="params">
        {CALL sp_file_update_thumbnail(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{atchGrup, mode=IN, jdbcType=VARCHAR},
            #{atchNo, mode=IN, jdbcType=VARCHAR},
            #{h_thumbnail, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [updateThumbNailNull] 썸네일 초기화 -->
    <update id="updateThumbNailNull" statementType="CALLABLE" parameterType="params">
        {CALL sp_file_update_thumbnail_null(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{atchGrup, mode=IN, jdbcType=VARCHAR},
            #{atchNo, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- [getNdaFileInfo] NDA 파일 정보 조회 (기존 프로시저 유지) -->
    <select id="getNdaFileInfo" resultType="file" parameterType="params">
        CALL sp_search_nda_file_info(#{sysId}, #{gsUserId}, #{atchGrup}, #{atchNo})
    </select>

    <!-- [updateFileComment] 파일 코멘트 수정 (기존 프로시저 유지) -->
    <update id="updateFileComment" parameterType="params">
        CALL sp_update_file_comment(#{sysId}, #{gsUserId}, #{atchGrup}, #{atchNo}, #{updateNo}, #{comment}, #{fileOper})
    </update>

    <!-- [insertFileChngHist] 파일 변경 이력 등록 (기존 프로시저 유지) -->
    <insert id="insertFileChngHist" parameterType="params">
        CALL sp_insert_file_chng_hist(#{sysId}, #{gsUserId}, #{tempId}, #{applId}, #{atchGrup}, #{atchNo}, #{updateNo}, #{fileName}, #{comment}, #{fileOper})
    </insert>

</mapper>
