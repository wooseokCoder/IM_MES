<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.code.Screenterm">

    <!-- ========================================================== -->
    <!--   Screenterm Procedures - CALLABLE 방식                     -->
    <!-- ========================================================== -->

    <!-- 목록 조회 (페이징) -->
    <select id="search" statementType="CALLABLE" resultType="record" parameterType="params">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_screenterm_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{itemGrpKey, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{itemId, mode=IN, jdbcType=VARCHAR},
            #{itemNm, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 목록 건수 조회 -->
    <select id="searchCount" statementType="CALLABLE" resultType="int" parameterType="params">
        {CALL sp_screenterm_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{itemGrpKey, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{itemId, mode=IN, jdbcType=VARCHAR},
            #{itemNm, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 단건 조회 -->
    <select id="select" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_screenterm_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{itemGrp, mode=IN, jdbcType=VARCHAR},
            #{itemId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 등록 -->
    <insert id="insert" statementType="CALLABLE" parameterType="params">
        {CALL sp_screenterm_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{itemGrp, mode=IN, jdbcType=VARCHAR},
            #{itemId, mode=IN, jdbcType=VARCHAR},
            #{itemNm, mode=IN, jdbcType=VARCHAR},
            #{itemNmKor, mode=IN, jdbcType=VARCHAR},
            #{itemNmEng, mode=IN, jdbcType=VARCHAR},
            #{itemNmPort, mode=IN, jdbcType=VARCHAR},
            #{itemNmViet, mode=IN, jdbcType=VARCHAR},
            #{itemNmEtc, mode=IN, jdbcType=VARCHAR},
            #{itemDesc, mode=IN, jdbcType=VARCHAR},
            #{itemType, mode=IN, jdbcType=VARCHAR},
            #{itemLen, mode=IN, jdbcType=VARCHAR},
            #{itemScale, mode=IN, jdbcType=VARCHAR},
            #{itemAtr1, mode=IN, jdbcType=VARCHAR},
            #{itemAtr2, mode=IN, jdbcType=VARCHAR},
            #{itemAtr3, mode=IN, jdbcType=VARCHAR},
            #{itemRemk, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- 수정 -->
    <update id="update" statementType="CALLABLE" parameterType="params">
        {CALL sp_screenterm_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{itemGrp, mode=IN, jdbcType=VARCHAR},
            #{itemId, mode=IN, jdbcType=VARCHAR},
            #{itemNm, mode=IN, jdbcType=VARCHAR},
            #{itemNmKor, mode=IN, jdbcType=VARCHAR},
            #{itemNmEng, mode=IN, jdbcType=VARCHAR},
            #{itemNmPort, mode=IN, jdbcType=VARCHAR},
            #{itemNmViet, mode=IN, jdbcType=VARCHAR},
            #{itemNmEtc, mode=IN, jdbcType=VARCHAR},
            #{itemDesc, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{itemType, mode=IN, jdbcType=VARCHAR},
            #{itemLen, mode=IN, jdbcType=VARCHAR},
            #{itemAtr1, mode=IN, jdbcType=VARCHAR},
            #{itemAtr2, mode=IN, jdbcType=VARCHAR},
            #{itemAtr3, mode=IN, jdbcType=VARCHAR},
            #{itemRemk, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- 삭제 -->
    <delete id="delete" statementType="CALLABLE" parameterType="params">
        {CALL sp_screenterm_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{itemGrp, mode=IN, jdbcType=VARCHAR},
            #{itemId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- 프로그램 키 목록 조회 -->
    <select id="selectProgKeyList" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_screenterm_prog_key_list(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- 다국어 명칭 조회 -->
    <select id="selectLangNm" statementType="CALLABLE" resultType="record" parameterType="params">
        {CALL sp_screenterm_lang_nm(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{itemKey, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

</mapper>
