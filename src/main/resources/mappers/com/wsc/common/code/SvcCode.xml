<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsc.common.code.SvcCode">

    <!-- ========================================================== -->
    <!--   서비스 코드 목록 조회 (페이징) - 프로시저 호출            -->
    <!-- ========================================================== -->
    <select id="search" resultType="code" parameterType="params" statementType="CALLABLE">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_svccode_search(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{codeDesc, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{sortSeq, mode=IN, jdbcType=VARCHAR},
            #{extChr01, mode=IN, jdbcType=VARCHAR},
            #{extChr02, mode=IN, jdbcType=VARCHAR},
            #{extChr03, mode=IN, jdbcType=VARCHAR},
            #{extChr04, mode=IN, jdbcType=VARCHAR},
            #{extChr05, mode=IN, jdbcType=VARCHAR},
            #{extChr06, mode=IN, jdbcType=VARCHAR},
            #{extChr07, mode=IN, jdbcType=VARCHAR},
            #{extChr08, mode=IN, jdbcType=VARCHAR},
            #{extChr09, mode=IN, jdbcType=VARCHAR},
            #{extChr10, mode=IN, jdbcType=VARCHAR},
            #{extNum01, mode=IN, jdbcType=VARCHAR},
            #{extNum02, mode=IN, jdbcType=VARCHAR},
            #{extNum03, mode=IN, jdbcType=VARCHAR},
            #{extNum04, mode=IN, jdbcType=VARCHAR},
            #{extNum05, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   서비스 코드 목록 카운트 - 프로시저 호출                   -->
    <!-- ========================================================== -->
    <select id="searchCount" resultType="int" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_search_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{codeDesc, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{sortSeq, mode=IN, jdbcType=VARCHAR},
            #{extChr01, mode=IN, jdbcType=VARCHAR},
            #{extChr02, mode=IN, jdbcType=VARCHAR},
            #{extChr03, mode=IN, jdbcType=VARCHAR},
            #{extChr04, mode=IN, jdbcType=VARCHAR},
            #{extChr05, mode=IN, jdbcType=VARCHAR},
            #{extChr06, mode=IN, jdbcType=VARCHAR},
            #{extChr07, mode=IN, jdbcType=VARCHAR},
            #{extChr08, mode=IN, jdbcType=VARCHAR},
            #{extChr09, mode=IN, jdbcType=VARCHAR},
            #{extChr10, mode=IN, jdbcType=VARCHAR},
            #{extNum01, mode=IN, jdbcType=VARCHAR},
            #{extNum02, mode=IN, jdbcType=VARCHAR},
            #{extNum03, mode=IN, jdbcType=VARCHAR},
            #{extNum04, mode=IN, jdbcType=VARCHAR},
            #{extNum05, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   서비스 코드 단건 조회 - 프로시저 호출                     -->
    <!-- ========================================================== -->
    <select id="select" resultType="code" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_select(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   서비스 코드 등록 - 프로시저 호출                          -->
    <!-- ========================================================== -->
    <insert id="insert" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_insert(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{codeGrup, mode=IN, jdbcType=VARCHAR},
            #{codeDesc, mode=IN, jdbcType=VARCHAR},
            #{sortSeq, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{extChr01, mode=IN, jdbcType=VARCHAR},
            #{extChr02, mode=IN, jdbcType=VARCHAR},
            #{extChr03, mode=IN, jdbcType=VARCHAR},
            #{extChr04, mode=IN, jdbcType=VARCHAR},
            #{extChr05, mode=IN, jdbcType=VARCHAR},
            #{extChr06, mode=IN, jdbcType=VARCHAR},
            #{extChr07, mode=IN, jdbcType=VARCHAR},
            #{extChr08, mode=IN, jdbcType=VARCHAR},
            #{extChr09, mode=IN, jdbcType=VARCHAR},
            #{extChr10, mode=IN, jdbcType=VARCHAR},
            #{extNum01, mode=IN, jdbcType=VARCHAR},
            #{extNum02, mode=IN, jdbcType=VARCHAR},
            #{extNum03, mode=IN, jdbcType=VARCHAR},
            #{extNum04, mode=IN, jdbcType=VARCHAR},
            #{extNum05, mode=IN, jdbcType=VARCHAR},
            #{extText, mode=IN, jdbcType=LONGVARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- ========================================================== -->
    <!--   서비스 코드 수정 - 프로시저 호출                          -->
    <!-- ========================================================== -->
    <update id="update" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_update(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{codeDesc, mode=IN, jdbcType=VARCHAR},
            #{sortSeq, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{extChr01, mode=IN, jdbcType=VARCHAR},
            #{extChr02, mode=IN, jdbcType=VARCHAR},
            #{extChr03, mode=IN, jdbcType=VARCHAR},
            #{extChr04, mode=IN, jdbcType=VARCHAR},
            #{extChr05, mode=IN, jdbcType=VARCHAR},
            #{extChr06, mode=IN, jdbcType=VARCHAR},
            #{extChr07, mode=IN, jdbcType=VARCHAR},
            #{extChr08, mode=IN, jdbcType=VARCHAR},
            #{extChr09, mode=IN, jdbcType=VARCHAR},
            #{extChr10, mode=IN, jdbcType=VARCHAR},
            #{extNum01, mode=IN, jdbcType=VARCHAR},
            #{extNum02, mode=IN, jdbcType=VARCHAR},
            #{extNum03, mode=IN, jdbcType=VARCHAR},
            #{extNum04, mode=IN, jdbcType=VARCHAR},
            #{extNum05, mode=IN, jdbcType=VARCHAR},
            #{extText, mode=IN, jdbcType=LONGVARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </update>

    <!-- ========================================================== -->
    <!--   서비스 코드 삭제 - 프로시저 호출                          -->
    <!-- ========================================================== -->
    <delete id="delete" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_delete(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   서비스 코드 전체 조회 - 프로시저 호출                     -->
    <!-- ========================================================== -->
    <select id="searchAll" resultType="code" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_search_all(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   서비스 코드 전체 삭제 - 프로시저 호출                     -->
    <!-- ========================================================== -->
    <delete id="deleteAll" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_delete_all(
            #{sysId, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   서비스 코드 이력 등록 - 프로시저 호출                     -->
    <!-- ========================================================== -->
    <insert id="insertHist" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_insert_hist(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{codeGrup, mode=IN, jdbcType=VARCHAR},
            #{codeDesc, mode=IN, jdbcType=VARCHAR},
            #{sortSeq, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{extChr01, mode=IN, jdbcType=VARCHAR},
            #{extChr02, mode=IN, jdbcType=VARCHAR},
            #{extChr03, mode=IN, jdbcType=VARCHAR},
            #{extChr04, mode=IN, jdbcType=VARCHAR},
            #{extChr05, mode=IN, jdbcType=VARCHAR},
            #{extChr06, mode=IN, jdbcType=VARCHAR},
            #{extChr07, mode=IN, jdbcType=VARCHAR},
            #{extChr08, mode=IN, jdbcType=VARCHAR},
            #{extChr09, mode=IN, jdbcType=VARCHAR},
            #{extChr10, mode=IN, jdbcType=VARCHAR},
            #{extNum01, mode=IN, jdbcType=VARCHAR},
            #{extNum02, mode=IN, jdbcType=VARCHAR},
            #{extNum03, mode=IN, jdbcType=VARCHAR},
            #{extNum04, mode=IN, jdbcType=VARCHAR},
            #{extNum05, mode=IN, jdbcType=VARCHAR},
            #{extText, mode=IN, jdbcType=LONGVARCHAR},
            #{histType, mode=IN, jdbcType=VARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- ========================================================== -->
    <!--   서비스 코드 기간 목록 조회 (페이징) - 프로시저 호출       -->
    <!-- ========================================================== -->
    <select id="searchTerm" resultType="code" parameterType="params" statementType="CALLABLE">
        <bind name="sortStr" value="gsSorts != null and gsSorts.size() > 0 ? @com.wsc.framework.utils.MybatisUtils@join(',', gsSorts) : null" />
        {CALL sp_svccode_search_term(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{codeGrup, mode=IN, jdbcType=VARCHAR},
            #{codeDesc, mode=IN, jdbcType=VARCHAR},
            #{codeDate, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{sortSeq, mode=IN, jdbcType=VARCHAR},
            #{extChr01, mode=IN, jdbcType=VARCHAR},
            #{extChr02, mode=IN, jdbcType=VARCHAR},
            #{extChr03, mode=IN, jdbcType=VARCHAR},
            #{extChr04, mode=IN, jdbcType=VARCHAR},
            #{extChr05, mode=IN, jdbcType=VARCHAR},
            #{extChr06, mode=IN, jdbcType=VARCHAR},
            #{extChr07, mode=IN, jdbcType=VARCHAR},
            #{extChr08, mode=IN, jdbcType=VARCHAR},
            #{extChr09, mode=IN, jdbcType=VARCHAR},
            #{extChr10, mode=IN, jdbcType=VARCHAR},
            #{extNum01, mode=IN, jdbcType=VARCHAR},
            #{extNum02, mode=IN, jdbcType=VARCHAR},
            #{extNum03, mode=IN, jdbcType=VARCHAR},
            #{extNum04, mode=IN, jdbcType=VARCHAR},
            #{extNum05, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR},
            #{sortStr, mode=IN, jdbcType=VARCHAR},
            #{start, mode=IN, jdbcType=VARCHAR},
            #{end, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   서비스 코드 기간 목록 카운트 - 프로시저 호출              -->
    <!-- ========================================================== -->
    <select id="searchTermCount" resultType="int" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_search_term_count(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{codeGrup, mode=IN, jdbcType=VARCHAR},
            #{codeDesc, mode=IN, jdbcType=VARCHAR},
            #{codeDate, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{sortSeq, mode=IN, jdbcType=VARCHAR},
            #{extChr01, mode=IN, jdbcType=VARCHAR},
            #{extChr02, mode=IN, jdbcType=VARCHAR},
            #{extChr03, mode=IN, jdbcType=VARCHAR},
            #{extChr04, mode=IN, jdbcType=VARCHAR},
            #{extChr05, mode=IN, jdbcType=VARCHAR},
            #{extChr06, mode=IN, jdbcType=VARCHAR},
            #{extChr07, mode=IN, jdbcType=VARCHAR},
            #{extChr08, mode=IN, jdbcType=VARCHAR},
            #{extChr09, mode=IN, jdbcType=VARCHAR},
            #{extChr10, mode=IN, jdbcType=VARCHAR},
            #{extNum01, mode=IN, jdbcType=VARCHAR},
            #{extNum02, mode=IN, jdbcType=VARCHAR},
            #{extNum03, mode=IN, jdbcType=VARCHAR},
            #{extNum04, mode=IN, jdbcType=VARCHAR},
            #{extNum05, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   서비스 코드 기간 단건 조회 - 프로시저 호출                -->
    <!-- ========================================================== -->
    <select id="selectTerm" resultType="code" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_select_term(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{gsLang, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   서비스 코드 기간 등록 - 프로시저 호출                     -->
    <!-- ========================================================== -->
    <insert id="insertTerm" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_insert_term(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{codeGrup, mode=IN, jdbcType=VARCHAR},
            #{codeDesc, mode=IN, jdbcType=VARCHAR},
            #{codeDate, mode=IN, jdbcType=VARCHAR},
            #{sortSeq, mode=IN, jdbcType=VARCHAR},
            #{useFlag, mode=IN, jdbcType=VARCHAR},
            #{extChr01, mode=IN, jdbcType=VARCHAR},
            #{extChr02, mode=IN, jdbcType=VARCHAR},
            #{extChr03, mode=IN, jdbcType=VARCHAR},
            #{extChr04, mode=IN, jdbcType=VARCHAR},
            #{extChr05, mode=IN, jdbcType=VARCHAR},
            #{extChr06, mode=IN, jdbcType=VARCHAR},
            #{extChr07, mode=IN, jdbcType=VARCHAR},
            #{extChr08, mode=IN, jdbcType=VARCHAR},
            #{extChr09, mode=IN, jdbcType=VARCHAR},
            #{extChr10, mode=IN, jdbcType=VARCHAR},
            #{extNum01, mode=IN, jdbcType=VARCHAR},
            #{extNum02, mode=IN, jdbcType=VARCHAR},
            #{extNum03, mode=IN, jdbcType=VARCHAR},
            #{extNum04, mode=IN, jdbcType=VARCHAR},
            #{extNum05, mode=IN, jdbcType=VARCHAR},
            #{extText, mode=IN, jdbcType=LONGVARCHAR},
            #{gsUserId, mode=IN, jdbcType=VARCHAR}
        )}
    </insert>

    <!-- ========================================================== -->
    <!--   서비스 코드 기간 삭제 - 프로시저 호출                     -->
    <!-- ========================================================== -->
    <delete id="deleteTerm" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_delete_term(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeCd, mode=IN, jdbcType=VARCHAR},
            #{codeDate, mode=IN, jdbcType=VARCHAR}
        )}
    </delete>

    <!-- ========================================================== -->
    <!--   AS 코드 조회 - 프로시저 호출                              -->
    <!-- ========================================================== -->
    <select id="searchCodeAs" resultType="record" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_search_code_as(
            #{sysId, mode=IN, jdbcType=VARCHAR},
            #{codeGrup2, mode=IN, jdbcType=VARCHAR}
        )}
    </select>

    <!-- ========================================================== -->
    <!--   고객 제품 조회 - 프로시저 호출                            -->
    <!-- ========================================================== -->
    <select id="searchCustProd" resultType="record" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_search_cust_prod()}
    </select>

    <!-- ========================================================== -->
    <!--   전체 제품 조회 - 프로시저 호출                            -->
    <!-- ========================================================== -->
    <select id="searchAllProd" resultType="record" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_search_all_prod()}
    </select>

    <!-- ========================================================== -->
    <!--   외주 고객 창고 조회 - 프로시저 호출                       -->
    <!-- ========================================================== -->
    <select id="searchOutsCustStrg" resultType="record" parameterType="params" statementType="CALLABLE">
        {CALL sp_svccode_search_outs_cust_strg()}
    </select>

</mapper>
